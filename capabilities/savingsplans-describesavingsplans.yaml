naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services savingsplans — DescribeSavingsPlans
  description: 'Amazon Web Services savingsplans — DescribeSavingsPlans. 1 operations. Lead operation: Amazon Web Services
    Describesavingsplans. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - DescribeSavingsPlans
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: savingsplans-describesavingsplans
    baseUri: ''
    description: Amazon Web Services savingsplans — DescribeSavingsPlans business capability. Self-contained, no shared references.
    resources:
    - name: DescribeSavingsPlans
      path: /DescribeSavingsPlans
      operations:
      - name: amazonwebservicesdescribesavingsplans
        method: POST
        description: Amazon Web Services Describesavingsplans
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: savingsplans-describesavingsplans-rest
    port: 8080
    description: REST adapter for Amazon Web Services savingsplans — DescribeSavingsPlans. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/describesavingsplans
      name: describesavingsplans
      description: REST surface for DescribeSavingsPlans.
      operations:
      - method: POST
        name: amazonwebservicesdescribesavingsplans
        description: Amazon Web Services Describesavingsplans
        call: savingsplans-describesavingsplans.amazonwebservicesdescribesavingsplans
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: savingsplans-describesavingsplans-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services savingsplans — DescribeSavingsPlans. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-describesavingsplans
      description: Amazon Web Services Describesavingsplans
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: savingsplans-describesavingsplans.amazonwebservicesdescribesavingsplans
      outputParameters:
      - type: object
        mapping: $.
