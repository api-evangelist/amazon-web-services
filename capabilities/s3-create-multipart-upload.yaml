naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services AWS S3 — Create Multipart Upload
  description: 'Amazon Web Services AWS S3 — Create Multipart Upload. 1 operations. Lead operation: Amazon Web Services Create
    Multipart Upload. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Create Multipart Upload
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: s3-create-multipart-upload
    baseUri: ''
    description: Amazon Web Services AWS S3 — Create Multipart Upload business capability. Self-contained, no shared references.
    resources:
    - name: Bucket-Key+}?uploads
      path: /{Bucket}/{Key+}?uploads
      operations:
      - name: createmultipartupload
        method: POST
        description: Amazon Web Services Create Multipart Upload
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: s3-create-multipart-upload-rest
    port: 8080
    description: REST adapter for Amazon Web Services AWS S3 — Create Multipart Upload. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/{bucket}/key-uploads
      name: bucket-key-uploads
      description: REST surface for Bucket-Key+}?uploads.
      operations:
      - method: POST
        name: createmultipartupload
        description: Amazon Web Services Create Multipart Upload
        call: s3-create-multipart-upload.createmultipartupload
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: s3-create-multipart-upload-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services AWS S3 — Create Multipart Upload. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-create-multipart
      description: Amazon Web Services Create Multipart Upload
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: s3-create-multipart-upload.createmultipartupload
      outputParameters:
      - type: object
        mapping: $.
