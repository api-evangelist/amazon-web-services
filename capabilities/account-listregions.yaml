naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services account — listRegions
  description: 'Amazon Web Services account — listRegions. 1 operations. Lead operation: Amazon Web Services Listregions.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - listRegions
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: account-listregions
    baseUri: ''
    description: Amazon Web Services account — listRegions business capability. Self-contained, no shared references.
    resources:
    - name: listRegions
      path: /listRegions
      operations:
      - name: amazonwebserviceslistregions
        method: POST
        description: Amazon Web Services Listregions
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: account-listregions-rest
    port: 8080
    description: REST adapter for Amazon Web Services account — listRegions. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/listregions
      name: listregions
      description: REST surface for listRegions.
      operations:
      - method: POST
        name: amazonwebserviceslistregions
        description: Amazon Web Services Listregions
        call: account-listregions.amazonwebserviceslistregions
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: account-listregions-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services account — listRegions. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: amazon-web-services-listregions
      description: Amazon Web Services Listregions
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: account-listregions.amazonwebserviceslistregions
      outputParameters:
      - type: object
        mapping: $.
