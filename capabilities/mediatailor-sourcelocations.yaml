naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services mediatailor — sourceLocations
  description: 'Amazon Web Services mediatailor — sourceLocations. 1 operations. Lead operation: Amazon Web Services Listsourcelocations.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - sourceLocations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: mediatailor-sourcelocations
    baseUri: ''
    description: Amazon Web Services mediatailor — sourceLocations business capability. Self-contained, no shared references.
    resources:
    - name: sourceLocations
      path: /sourceLocations
      operations:
      - name: amazonwebserviceslistsourcelocations
        method: GET
        description: Amazon Web Services Listsourcelocations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: mediatailor-sourcelocations-rest
    port: 8080
    description: REST adapter for Amazon Web Services mediatailor — sourceLocations. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/sourcelocations
      name: sourcelocations
      description: REST surface for sourceLocations.
      operations:
      - method: GET
        name: amazonwebserviceslistsourcelocations
        description: Amazon Web Services Listsourcelocations
        call: mediatailor-sourcelocations.amazonwebserviceslistsourcelocations
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: mediatailor-sourcelocations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services mediatailor — sourceLocations. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-listsourcelocations
      description: Amazon Web Services Listsourcelocations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: mediatailor-sourcelocations.amazonwebserviceslistsourcelocations
      outputParameters:
      - type: object
        mapping: $.
