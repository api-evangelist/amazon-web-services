naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services xray — EncryptionConfig
  description: 'Amazon Web Services xray — EncryptionConfig. 1 operations. Lead operation: Amazon Web Services Getencryptionconfig.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - EncryptionConfig
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: xray-encryptionconfig
    baseUri: ''
    description: Amazon Web Services xray — EncryptionConfig business capability. Self-contained, no shared references.
    resources:
    - name: EncryptionConfig
      path: /EncryptionConfig
      operations:
      - name: amazonwebservicesgetencryptionconfig
        method: POST
        description: Amazon Web Services Getencryptionconfig
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: xray-encryptionconfig-rest
    port: 8080
    description: REST adapter for Amazon Web Services xray — EncryptionConfig. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/encryptionconfig
      name: encryptionconfig
      description: REST surface for EncryptionConfig.
      operations:
      - method: POST
        name: amazonwebservicesgetencryptionconfig
        description: Amazon Web Services Getencryptionconfig
        call: xray-encryptionconfig.amazonwebservicesgetencryptionconfig
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: xray-encryptionconfig-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services xray — EncryptionConfig. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: amazon-web-services-getencryptionconfig
      description: Amazon Web Services Getencryptionconfig
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: xray-encryptionconfig.amazonwebservicesgetencryptionconfig
      outputParameters:
      - type: object
        mapping: $.
