naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services auditmanager — dataSourceKeywords
  description: 'Amazon Web Services auditmanager — dataSourceKeywords. 1 operations. Lead operation: Amazon Web Services Listkeywordsfordatasource.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - dataSourceKeywords
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: auditmanager-datasourcekeywords
    baseUri: ''
    description: Amazon Web Services auditmanager — dataSourceKeywords business capability. Self-contained, no shared references.
    resources:
    - name: dataSourceKeywords
      path: /dataSourceKeywords
      operations:
      - name: amazonwebserviceslistkeywordsfordatasource
        method: GET
        description: Amazon Web Services Listkeywordsfordatasource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: auditmanager-datasourcekeywords-rest
    port: 8080
    description: REST adapter for Amazon Web Services auditmanager — dataSourceKeywords. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/datasourcekeywords
      name: datasourcekeywords
      description: REST surface for dataSourceKeywords.
      operations:
      - method: GET
        name: amazonwebserviceslistkeywordsfordatasource
        description: Amazon Web Services Listkeywordsfordatasource
        call: auditmanager-datasourcekeywords.amazonwebserviceslistkeywordsfordatasource
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: auditmanager-datasourcekeywords-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services auditmanager — dataSourceKeywords. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-listkeywordsfordatasource
      description: Amazon Web Services Listkeywordsfordatasource
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: auditmanager-datasourcekeywords.amazonwebserviceslistkeywordsfordatasource
      outputParameters:
      - type: object
        mapping: $.
