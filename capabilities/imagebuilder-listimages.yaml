naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services imagebuilder — ListImages
  description: 'Amazon Web Services imagebuilder — ListImages. 1 operations. Lead operation: Amazon Web Services Listimages.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - ListImages
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: imagebuilder-listimages
    baseUri: ''
    description: Amazon Web Services imagebuilder — ListImages business capability. Self-contained, no shared references.
    resources:
    - name: ListImages
      path: /ListImages
      operations:
      - name: amazonwebserviceslistimages
        method: POST
        description: Amazon Web Services Listimages
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: imagebuilder-listimages-rest
    port: 8080
    description: REST adapter for Amazon Web Services imagebuilder — ListImages. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/listimages
      name: listimages
      description: REST surface for ListImages.
      operations:
      - method: POST
        name: amazonwebserviceslistimages
        description: Amazon Web Services Listimages
        call: imagebuilder-listimages.amazonwebserviceslistimages
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: imagebuilder-listimages-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services imagebuilder — ListImages. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: amazon-web-services-listimages
      description: Amazon Web Services Listimages
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: imagebuilder-listimages.amazonwebserviceslistimages
      outputParameters:
      - type: object
        mapping: $.
