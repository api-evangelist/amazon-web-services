naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services mgn — ResumeReplication
  description: 'Amazon Web Services mgn — ResumeReplication. 1 operations. Lead operation: Amazon Web Services Resumereplication.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - ResumeReplication
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: mgn-resumereplication
    baseUri: ''
    description: Amazon Web Services mgn — ResumeReplication business capability. Self-contained, no shared references.
    resources:
    - name: ResumeReplication
      path: /ResumeReplication
      operations:
      - name: amazonwebservicesresumereplication
        method: POST
        description: Amazon Web Services Resumereplication
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: mgn-resumereplication-rest
    port: 8080
    description: REST adapter for Amazon Web Services mgn — ResumeReplication. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/resumereplication
      name: resumereplication
      description: REST surface for ResumeReplication.
      operations:
      - method: POST
        name: amazonwebservicesresumereplication
        description: Amazon Web Services Resumereplication
        call: mgn-resumereplication.amazonwebservicesresumereplication
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: mgn-resumereplication-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services mgn — ResumeReplication. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: amazon-web-services-resumereplication
      description: Amazon Web Services Resumereplication
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: mgn-resumereplication.amazonwebservicesresumereplication
      outputParameters:
      - type: object
        mapping: $.
