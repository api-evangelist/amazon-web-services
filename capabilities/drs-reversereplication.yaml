naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services drs — ReverseReplication
  description: 'Amazon Web Services drs — ReverseReplication. 1 operations. Lead operation: Amazon Web Services Reversereplication.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - ReverseReplication
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: drs-reversereplication
    baseUri: ''
    description: Amazon Web Services drs — ReverseReplication business capability. Self-contained, no shared references.
    resources:
    - name: ReverseReplication
      path: /ReverseReplication
      operations:
      - name: amazonwebservicesreversereplication
        method: POST
        description: Amazon Web Services Reversereplication
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: drs-reversereplication-rest
    port: 8080
    description: REST adapter for Amazon Web Services drs — ReverseReplication. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/reversereplication
      name: reversereplication
      description: REST surface for ReverseReplication.
      operations:
      - method: POST
        name: amazonwebservicesreversereplication
        description: Amazon Web Services Reversereplication
        call: drs-reversereplication.amazonwebservicesreversereplication
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: drs-reversereplication-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services drs — ReverseReplication. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: amazon-web-services-reversereplication
      description: Amazon Web Services Reversereplication
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: drs-reversereplication.amazonwebservicesreversereplication
      outputParameters:
      - type: object
        mapping: $.
