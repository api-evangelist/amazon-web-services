naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services lakeformation — GetTemporaryGlueTableCredentials
  description: 'Amazon Web Services lakeformation — GetTemporaryGlueTableCredentials. 1 operations. Lead operation: Amazon
    Web Services Gettemporarygluetablecredentials. Self-contained Naftiko capability covering one Amazon Web Services business
    surface.'
  tags:
  - Amazon Web Services
  - GetTemporaryGlueTableCredentials
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: lakeformation-gettemporarygluetablecredentials
    baseUri: ''
    description: Amazon Web Services lakeformation — GetTemporaryGlueTableCredentials business capability. Self-contained,
      no shared references.
    resources:
    - name: GetTemporaryGlueTableCredentials
      path: /GetTemporaryGlueTableCredentials
      operations:
      - name: amazonwebservicesgettemporarygluetablecredentials
        method: POST
        description: Amazon Web Services Gettemporarygluetablecredentials
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: lakeformation-gettemporarygluetablecredentials-rest
    port: 8080
    description: REST adapter for Amazon Web Services lakeformation — GetTemporaryGlueTableCredentials. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/gettemporarygluetablecredentials
      name: gettemporarygluetablecredentials
      description: REST surface for GetTemporaryGlueTableCredentials.
      operations:
      - method: POST
        name: amazonwebservicesgettemporarygluetablecredentials
        description: Amazon Web Services Gettemporarygluetablecredentials
        call: lakeformation-gettemporarygluetablecredentials.amazonwebservicesgettemporarygluetablecredentials
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: lakeformation-gettemporarygluetablecredentials-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services lakeformation — GetTemporaryGlueTableCredentials. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-gettemporarygluetablecredentials
      description: Amazon Web Services Gettemporarygluetablecredentials
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: lakeformation-gettemporarygluetablecredentials.amazonwebservicesgettemporarygluetablecredentials
      outputParameters:
      - type: object
        mapping: $.
