naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services ivs — StartViewerSessionRevocation
  description: 'Amazon Web Services ivs — StartViewerSessionRevocation. 1 operations. Lead operation: Amazon Web Services
    Startviewersessionrevocation. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - StartViewerSessionRevocation
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: ivs-startviewersessionrevocation
    baseUri: ''
    description: Amazon Web Services ivs — StartViewerSessionRevocation business capability. Self-contained, no shared references.
    resources:
    - name: StartViewerSessionRevocation
      path: /StartViewerSessionRevocation
      operations:
      - name: amazonwebservicesstartviewersessionrevocation
        method: POST
        description: Amazon Web Services Startviewersessionrevocation
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: ivs-startviewersessionrevocation-rest
    port: 8080
    description: REST adapter for Amazon Web Services ivs — StartViewerSessionRevocation. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/startviewersessionrevocation
      name: startviewersessionrevocation
      description: REST surface for StartViewerSessionRevocation.
      operations:
      - method: POST
        name: amazonwebservicesstartviewersessionrevocation
        description: Amazon Web Services Startviewersessionrevocation
        call: ivs-startviewersessionrevocation.amazonwebservicesstartviewersessionrevocation
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ivs-startviewersessionrevocation-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services ivs — StartViewerSessionRevocation. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-startviewersessionrevocation
      description: Amazon Web Services Startviewersessionrevocation
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ivs-startviewersessionrevocation.amazonwebservicesstartviewersessionrevocation
      outputParameters:
      - type: object
        mapping: $.
