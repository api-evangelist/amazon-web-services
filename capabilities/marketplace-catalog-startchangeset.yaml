naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services marketplace-catalog — StartChangeSet
  description: 'Amazon Web Services marketplace-catalog — StartChangeSet. 1 operations. Lead operation: Amazon Web Services
    Startchangeset. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - StartChangeSet
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: marketplace-catalog-startchangeset
    baseUri: ''
    description: Amazon Web Services marketplace-catalog — StartChangeSet business capability. Self-contained, no shared references.
    resources:
    - name: StartChangeSet
      path: /StartChangeSet
      operations:
      - name: amazonwebservicesstartchangeset
        method: POST
        description: Amazon Web Services Startchangeset
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: marketplace-catalog-startchangeset-rest
    port: 8080
    description: REST adapter for Amazon Web Services marketplace-catalog — StartChangeSet. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/startchangeset
      name: startchangeset
      description: REST surface for StartChangeSet.
      operations:
      - method: POST
        name: amazonwebservicesstartchangeset
        description: Amazon Web Services Startchangeset
        call: marketplace-catalog-startchangeset.amazonwebservicesstartchangeset
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: marketplace-catalog-startchangeset-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services marketplace-catalog — StartChangeSet. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-startchangeset
      description: Amazon Web Services Startchangeset
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: marketplace-catalog-startchangeset.amazonwebservicesstartchangeset
      outputParameters:
      - type: object
        mapping: $.
