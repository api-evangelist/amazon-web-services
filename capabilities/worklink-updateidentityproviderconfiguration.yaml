naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services worklink — updateIdentityProviderConfiguration
  description: 'Amazon Web Services worklink — updateIdentityProviderConfiguration. 1 operations. Lead operation: Amazon Web
    Services Updateidentityproviderconfiguration. Self-contained Naftiko capability covering one Amazon Web Services business
    surface.'
  tags:
  - Amazon Web Services
  - updateIdentityProviderConfiguration
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: worklink-updateidentityproviderconfiguration
    baseUri: ''
    description: Amazon Web Services worklink — updateIdentityProviderConfiguration business capability. Self-contained, no
      shared references.
    resources:
    - name: updateIdentityProviderConfiguration
      path: /updateIdentityProviderConfiguration
      operations:
      - name: amazonwebservicesupdateidentityproviderconfiguration
        method: POST
        description: Amazon Web Services Updateidentityproviderconfiguration
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: worklink-updateidentityproviderconfiguration-rest
    port: 8080
    description: REST adapter for Amazon Web Services worklink — updateIdentityProviderConfiguration. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/updateidentityproviderconfiguration
      name: updateidentityproviderconfiguration
      description: REST surface for updateIdentityProviderConfiguration.
      operations:
      - method: POST
        name: amazonwebservicesupdateidentityproviderconfiguration
        description: Amazon Web Services Updateidentityproviderconfiguration
        call: worklink-updateidentityproviderconfiguration.amazonwebservicesupdateidentityproviderconfiguration
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: worklink-updateidentityproviderconfiguration-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services worklink — updateIdentityProviderConfiguration. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-updateidentityproviderconfiguration
      description: Amazon Web Services Updateidentityproviderconfiguration
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: worklink-updateidentityproviderconfiguration.amazonwebservicesupdateidentityproviderconfiguration
      outputParameters:
      - type: object
        mapping: $.
