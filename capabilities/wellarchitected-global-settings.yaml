naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services wellarchitected — Global Settings
  description: 'Amazon Web Services wellarchitected — Global Settings. 1 operations. Lead operation: Amazon Web Services Updateglobalsettings.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Global Settings
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: wellarchitected-global-settings
    baseUri: ''
    description: Amazon Web Services wellarchitected — Global Settings business capability. Self-contained, no shared references.
    resources:
    - name: global-settings
      path: /global-settings
      operations:
      - name: amazonwebservicesupdateglobalsettings
        method: PATCH
        description: Amazon Web Services Updateglobalsettings
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: wellarchitected-global-settings-rest
    port: 8080
    description: REST adapter for Amazon Web Services wellarchitected — Global Settings. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/global-settings
      name: global-settings
      description: REST surface for global-settings.
      operations:
      - method: PATCH
        name: amazonwebservicesupdateglobalsettings
        description: Amazon Web Services Updateglobalsettings
        call: wellarchitected-global-settings.amazonwebservicesupdateglobalsettings
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: wellarchitected-global-settings-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services wellarchitected — Global Settings. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-updateglobalsettings
      description: Amazon Web Services Updateglobalsettings
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: wellarchitected-global-settings.amazonwebservicesupdateglobalsettings
      outputParameters:
      - type: object
        mapping: $.
