naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services mgn — FinalizeCutover
  description: 'Amazon Web Services mgn — FinalizeCutover. 1 operations. Lead operation: Amazon Web Services Finalizecutover.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - FinalizeCutover
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: mgn-finalizecutover
    baseUri: ''
    description: Amazon Web Services mgn — FinalizeCutover business capability. Self-contained, no shared references.
    resources:
    - name: FinalizeCutover
      path: /FinalizeCutover
      operations:
      - name: amazonwebservicesfinalizecutover
        method: POST
        description: Amazon Web Services Finalizecutover
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: mgn-finalizecutover-rest
    port: 8080
    description: REST adapter for Amazon Web Services mgn — FinalizeCutover. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/finalizecutover
      name: finalizecutover
      description: REST surface for FinalizeCutover.
      operations:
      - method: POST
        name: amazonwebservicesfinalizecutover
        description: Amazon Web Services Finalizecutover
        call: mgn-finalizecutover.amazonwebservicesfinalizecutover
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: mgn-finalizecutover-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services mgn — FinalizeCutover. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: amazon-web-services-finalizecutover
      description: Amazon Web Services Finalizecutover
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: mgn-finalizecutover.amazonwebservicesfinalizecutover
      outputParameters:
      - type: object
        mapping: $.
