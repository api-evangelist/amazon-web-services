naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services iot — Cacertificate
  description: 'Amazon Web Services iot — Cacertificate. 2 operations. Lead operation: Amazon Web Services Registercacertificate.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Cacertificate
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: iot-cacertificate
    baseUri: ''
    description: Amazon Web Services iot — Cacertificate business capability. Self-contained, no shared references.
    resources:
    - name: cacertificate
      path: /cacertificate
      operations:
      - name: amazonwebservicesregistercacertificate
        method: POST
        description: Amazon Web Services Registercacertificate
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: cacertificate-caCertificateId
      path: /cacertificate/{caCertificateId}
      operations:
      - name: amazonwebservicesupdatecacertificate
        method: PUT
        description: Amazon Web Services Updatecacertificate
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: iot-cacertificate-rest
    port: 8080
    description: REST adapter for Amazon Web Services iot — Cacertificate. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/cacertificate
      name: cacertificate
      description: REST surface for cacertificate.
      operations:
      - method: POST
        name: amazonwebservicesregistercacertificate
        description: Amazon Web Services Registercacertificate
        call: iot-cacertificate.amazonwebservicesregistercacertificate
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/cacertificate/{cacertificateid}
      name: cacertificate-cacertificateid
      description: REST surface for cacertificate-caCertificateId.
      operations:
      - method: PUT
        name: amazonwebservicesupdatecacertificate
        description: Amazon Web Services Updatecacertificate
        call: iot-cacertificate.amazonwebservicesupdatecacertificate
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: iot-cacertificate-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services iot — Cacertificate. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: amazon-web-services-registercacertificate
      description: Amazon Web Services Registercacertificate
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: iot-cacertificate.amazonwebservicesregistercacertificate
      outputParameters:
      - type: object
        mapping: $.
    - name: amazon-web-services-updatecacertificate
      description: Amazon Web Services Updatecacertificate
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: iot-cacertificate.amazonwebservicesupdatecacertificate
      outputParameters:
      - type: object
        mapping: $.
