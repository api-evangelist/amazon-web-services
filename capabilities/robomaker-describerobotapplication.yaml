naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services robomaker — describeRobotApplication
  description: 'Amazon Web Services robomaker — describeRobotApplication. 1 operations. Lead operation: Amazon Web Services
    Describerobotapplication. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - describeRobotApplication
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: robomaker-describerobotapplication
    baseUri: ''
    description: Amazon Web Services robomaker — describeRobotApplication business capability. Self-contained, no shared references.
    resources:
    - name: describeRobotApplication
      path: /describeRobotApplication
      operations:
      - name: amazonwebservicesdescriberobotapplication
        method: POST
        description: Amazon Web Services Describerobotapplication
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: robomaker-describerobotapplication-rest
    port: 8080
    description: REST adapter for Amazon Web Services robomaker — describeRobotApplication. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/describerobotapplication
      name: describerobotapplication
      description: REST surface for describeRobotApplication.
      operations:
      - method: POST
        name: amazonwebservicesdescriberobotapplication
        description: Amazon Web Services Describerobotapplication
        call: robomaker-describerobotapplication.amazonwebservicesdescriberobotapplication
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: robomaker-describerobotapplication-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services robomaker — describeRobotApplication. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-describerobotapplication
      description: Amazon Web Services Describerobotapplication
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: robomaker-describerobotapplication.amazonwebservicesdescriberobotapplication
      outputParameters:
      - type: object
        mapping: $.
