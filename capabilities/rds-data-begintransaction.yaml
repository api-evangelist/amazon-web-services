naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services rds-data — BeginTransaction
  description: 'Amazon Web Services rds-data — BeginTransaction. 1 operations. Lead operation: Amazon Web Services Begintransaction.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - BeginTransaction
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: rds-data-begintransaction
    baseUri: ''
    description: Amazon Web Services rds-data — BeginTransaction business capability. Self-contained, no shared references.
    resources:
    - name: BeginTransaction
      path: /BeginTransaction
      operations:
      - name: amazonwebservicesbegintransaction
        method: POST
        description: Amazon Web Services Begintransaction
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: rds-data-begintransaction-rest
    port: 8080
    description: REST adapter for Amazon Web Services rds-data — BeginTransaction. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/begintransaction
      name: begintransaction
      description: REST surface for BeginTransaction.
      operations:
      - method: POST
        name: amazonwebservicesbegintransaction
        description: Amazon Web Services Begintransaction
        call: rds-data-begintransaction.amazonwebservicesbegintransaction
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: rds-data-begintransaction-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services rds-data — BeginTransaction. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-begintransaction
      description: Amazon Web Services Begintransaction
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: rds-data-begintransaction.amazonwebservicesbegintransaction
      outputParameters:
      - type: object
        mapping: $.
