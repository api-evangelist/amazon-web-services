naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services bedrock-agent-runtime — Agents
  description: 'Amazon Web Services bedrock-agent-runtime — Agents. 1 operations. Lead operation: Amazon Web Services Invokeagent.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Agents
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: bedrock-agent-runtime-agents
    baseUri: ''
    description: Amazon Web Services bedrock-agent-runtime — Agents business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-agentAliases-agentAliasId-sessions-sessionId-text
      path: /agents/{agentId}/agentAliases/{agentAliasId}/sessions/{sessionId}/text
      operations:
      - name: amazonwebservicesinvokeagent
        method: POST
        description: Amazon Web Services Invokeagent
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: bedrock-agent-runtime-agents-rest
    port: 8080
    description: REST adapter for Amazon Web Services bedrock-agent-runtime — Agents. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/agentaliases/{agentaliasid}/sessions/{sessionid}/text
      name: agents-agentid-agentaliases-agentaliasid-sessions-sessionid-text
      description: REST surface for agents-agentId-agentAliases-agentAliasId-sessions-sessionId-text.
      operations:
      - method: POST
        name: amazonwebservicesinvokeagent
        description: Amazon Web Services Invokeagent
        call: bedrock-agent-runtime-agents.amazonwebservicesinvokeagent
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: bedrock-agent-runtime-agents-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services bedrock-agent-runtime — Agents. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-invokeagent
      description: Amazon Web Services Invokeagent
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: bedrock-agent-runtime-agents.amazonwebservicesinvokeagent
      outputParameters:
      - type: object
        mapping: $.
