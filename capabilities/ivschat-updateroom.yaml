naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services ivschat — UpdateRoom
  description: 'Amazon Web Services ivschat — UpdateRoom. 1 operations. Lead operation: Amazon Web Services Updateroom. Self-contained
    Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - UpdateRoom
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: ivschat-updateroom
    baseUri: ''
    description: Amazon Web Services ivschat — UpdateRoom business capability. Self-contained, no shared references.
    resources:
    - name: UpdateRoom
      path: /UpdateRoom
      operations:
      - name: amazonwebservicesupdateroom
        method: POST
        description: Amazon Web Services Updateroom
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: ivschat-updateroom-rest
    port: 8080
    description: REST adapter for Amazon Web Services ivschat — UpdateRoom. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/updateroom
      name: updateroom
      description: REST surface for UpdateRoom.
      operations:
      - method: POST
        name: amazonwebservicesupdateroom
        description: Amazon Web Services Updateroom
        call: ivschat-updateroom.amazonwebservicesupdateroom
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ivschat-updateroom-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services ivschat — UpdateRoom. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: amazon-web-services-updateroom
      description: Amazon Web Services Updateroom
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ivschat-updateroom.amazonwebservicesupdateroom
      outputParameters:
      - type: object
        mapping: $.
