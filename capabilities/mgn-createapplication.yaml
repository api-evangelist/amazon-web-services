naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services mgn — CreateApplication
  description: 'Amazon Web Services mgn — CreateApplication. 1 operations. Lead operation: Amazon Web Services Createapplication.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - CreateApplication
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: mgn-createapplication
    baseUri: ''
    description: Amazon Web Services mgn — CreateApplication business capability. Self-contained, no shared references.
    resources:
    - name: CreateApplication
      path: /CreateApplication
      operations:
      - name: amazonwebservicescreateapplication
        method: POST
        description: Amazon Web Services Createapplication
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: mgn-createapplication-rest
    port: 8080
    description: REST adapter for Amazon Web Services mgn — CreateApplication. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/createapplication
      name: createapplication
      description: REST surface for CreateApplication.
      operations:
      - method: POST
        name: amazonwebservicescreateapplication
        description: Amazon Web Services Createapplication
        call: mgn-createapplication.amazonwebservicescreateapplication
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: mgn-createapplication-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services mgn — CreateApplication. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: amazon-web-services-createapplication
      description: Amazon Web Services Createapplication
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: mgn-createapplication.amazonwebservicescreateapplication
      outputParameters:
      - type: object
        mapping: $.
