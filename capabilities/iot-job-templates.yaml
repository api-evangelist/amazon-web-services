naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services iot — Job Templates
  description: 'Amazon Web Services iot — Job Templates. 2 operations. Lead operation: Amazon Web Services Listjobtemplates.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Job Templates
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: iot-job-templates
    baseUri: ''
    description: Amazon Web Services iot — Job Templates business capability. Self-contained, no shared references.
    resources:
    - name: job-templates
      path: /job-templates
      operations:
      - name: amazonwebserviceslistjobtemplates
        method: GET
        description: Amazon Web Services Listjobtemplates
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: job-templates-jobTemplateId
      path: /job-templates/{jobTemplateId}
      operations:
      - name: amazonwebservicesdescribejobtemplate
        method: GET
        description: Amazon Web Services Describejobtemplate
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: iot-job-templates-rest
    port: 8080
    description: REST adapter for Amazon Web Services iot — Job Templates. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/job-templates
      name: job-templates
      description: REST surface for job-templates.
      operations:
      - method: GET
        name: amazonwebserviceslistjobtemplates
        description: Amazon Web Services Listjobtemplates
        call: iot-job-templates.amazonwebserviceslistjobtemplates
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/job-templates/{jobtemplateid}
      name: job-templates-jobtemplateid
      description: REST surface for job-templates-jobTemplateId.
      operations:
      - method: GET
        name: amazonwebservicesdescribejobtemplate
        description: Amazon Web Services Describejobtemplate
        call: iot-job-templates.amazonwebservicesdescribejobtemplate
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: iot-job-templates-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services iot — Job Templates. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: amazon-web-services-listjobtemplates
      description: Amazon Web Services Listjobtemplates
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: iot-job-templates.amazonwebserviceslistjobtemplates
      outputParameters:
      - type: object
        mapping: $.
    - name: amazon-web-services-describejobtemplate
      description: Amazon Web Services Describejobtemplate
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: iot-job-templates.amazonwebservicesdescribejobtemplate
      outputParameters:
      - type: object
        mapping: $.
