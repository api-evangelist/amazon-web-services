naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services drs — StopSourceNetworkReplication
  description: 'Amazon Web Services drs — StopSourceNetworkReplication. 1 operations. Lead operation: Amazon Web Services
    Stopsourcenetworkreplication. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - StopSourceNetworkReplication
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: drs-stopsourcenetworkreplication
    baseUri: ''
    description: Amazon Web Services drs — StopSourceNetworkReplication business capability. Self-contained, no shared references.
    resources:
    - name: StopSourceNetworkReplication
      path: /StopSourceNetworkReplication
      operations:
      - name: amazonwebservicesstopsourcenetworkreplication
        method: POST
        description: Amazon Web Services Stopsourcenetworkreplication
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: drs-stopsourcenetworkreplication-rest
    port: 8080
    description: REST adapter for Amazon Web Services drs — StopSourceNetworkReplication. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/stopsourcenetworkreplication
      name: stopsourcenetworkreplication
      description: REST surface for StopSourceNetworkReplication.
      operations:
      - method: POST
        name: amazonwebservicesstopsourcenetworkreplication
        description: Amazon Web Services Stopsourcenetworkreplication
        call: drs-stopsourcenetworkreplication.amazonwebservicesstopsourcenetworkreplication
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: drs-stopsourcenetworkreplication-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services drs — StopSourceNetworkReplication. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-stopsourcenetworkreplication
      description: Amazon Web Services Stopsourcenetworkreplication
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: drs-stopsourcenetworkreplication.amazonwebservicesstopsourcenetworkreplication
      outputParameters:
      - type: object
        mapping: $.
