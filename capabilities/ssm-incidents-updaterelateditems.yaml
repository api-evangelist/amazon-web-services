naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services ssm-incidents — updateRelatedItems
  description: 'Amazon Web Services ssm-incidents — updateRelatedItems. 1 operations. Lead operation: Amazon Web Services
    Updaterelateditems. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - updateRelatedItems
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: ssm-incidents-updaterelateditems
    baseUri: ''
    description: Amazon Web Services ssm-incidents — updateRelatedItems business capability. Self-contained, no shared references.
    resources:
    - name: updateRelatedItems
      path: /updateRelatedItems
      operations:
      - name: amazonwebservicesupdaterelateditems
        method: POST
        description: Amazon Web Services Updaterelateditems
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: ssm-incidents-updaterelateditems-rest
    port: 8080
    description: REST adapter for Amazon Web Services ssm-incidents — updateRelatedItems. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/updaterelateditems
      name: updaterelateditems
      description: REST surface for updateRelatedItems.
      operations:
      - method: POST
        name: amazonwebservicesupdaterelateditems
        description: Amazon Web Services Updaterelateditems
        call: ssm-incidents-updaterelateditems.amazonwebservicesupdaterelateditems
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ssm-incidents-updaterelateditems-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services ssm-incidents — updateRelatedItems. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-updaterelateditems
      description: Amazon Web Services Updaterelateditems
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ssm-incidents-updaterelateditems.amazonwebservicesupdaterelateditems
      outputParameters:
      - type: object
        mapping: $.
