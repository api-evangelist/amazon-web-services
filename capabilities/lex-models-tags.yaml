naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services lex-models — Tags
  description: 'Amazon Web Services lex-models — Tags. 1 operations. Lead operation: Amazon Web Services Untagresource. Self-contained
    Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Tags
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: lex-models-tags
    baseUri: ''
    description: Amazon Web Services lex-models — Tags business capability. Self-contained, no shared references.
    resources:
    - name: tags-resourceArn
      path: /tags/{resourceArn}
      operations:
      - name: amazonwebservicesuntagresource
        method: DELETE
        description: Amazon Web Services Untagresource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: lex-models-tags-rest
    port: 8080
    description: REST adapter for Amazon Web Services lex-models — Tags. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/tags/{resourcearn}
      name: tags-resourcearn
      description: REST surface for tags-resourceArn.
      operations:
      - method: DELETE
        name: amazonwebservicesuntagresource
        description: Amazon Web Services Untagresource
        call: lex-models-tags.amazonwebservicesuntagresource
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: lex-models-tags-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services lex-models — Tags. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: amazon-web-services-untagresource
      description: Amazon Web Services Untagresource
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: lex-models-tags.amazonwebservicesuntagresource
      outputParameters:
      - type: object
        mapping: $.
