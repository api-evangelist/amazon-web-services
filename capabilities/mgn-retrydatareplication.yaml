naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services mgn — RetryDataReplication
  description: 'Amazon Web Services mgn — RetryDataReplication. 1 operations. Lead operation: Amazon Web Services Retrydatareplication.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - RetryDataReplication
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: mgn-retrydatareplication
    baseUri: ''
    description: Amazon Web Services mgn — RetryDataReplication business capability. Self-contained, no shared references.
    resources:
    - name: RetryDataReplication
      path: /RetryDataReplication
      operations:
      - name: amazonwebservicesretrydatareplication
        method: POST
        description: Amazon Web Services Retrydatareplication
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: mgn-retrydatareplication-rest
    port: 8080
    description: REST adapter for Amazon Web Services mgn — RetryDataReplication. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/retrydatareplication
      name: retrydatareplication
      description: REST surface for RetryDataReplication.
      operations:
      - method: POST
        name: amazonwebservicesretrydatareplication
        description: Amazon Web Services Retrydatareplication
        call: mgn-retrydatareplication.amazonwebservicesretrydatareplication
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: mgn-retrydatareplication-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services mgn — RetryDataReplication. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: amazon-web-services-retrydatareplication
      description: Amazon Web Services Retrydatareplication
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: mgn-retrydatareplication.amazonwebservicesretrydatareplication
      outputParameters:
      - type: object
        mapping: $.
