naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services apigateway — Restapis?mode=import
  description: 'Amazon Web Services apigateway — Restapis?mode=import. 1 operations. Lead operation: Amazon Web Services Importrestapi.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Restapis?mode=import
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: apigateway-restapis-mode-import
    baseUri: ''
    description: Amazon Web Services apigateway — Restapis?mode=import business capability. Self-contained, no shared references.
    resources:
    - name: restapis?mode=import
      path: /restapis?mode=import
      operations:
      - name: amazonwebservicesimportrestapi
        method: POST
        description: Amazon Web Services Importrestapi
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: apigateway-restapis-mode-import-rest
    port: 8080
    description: REST adapter for Amazon Web Services apigateway — Restapis?mode=import. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/restapis-mode-import
      name: restapis-mode-import
      description: REST surface for restapis?mode=import.
      operations:
      - method: POST
        name: amazonwebservicesimportrestapi
        description: Amazon Web Services Importrestapi
        call: apigateway-restapis-mode-import.amazonwebservicesimportrestapi
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: apigateway-restapis-mode-import-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services apigateway — Restapis?mode=import. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-importrestapi
      description: Amazon Web Services Importrestapi
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: apigateway-restapis-mode-import.amazonwebservicesimportrestapi
      outputParameters:
      - type: object
        mapping: $.
