naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services chime-sdk-voice — Voice Connector Regions
  description: 'Amazon Web Services chime-sdk-voice — Voice Connector Regions. 1 operations. Lead operation: Amazon Web Services
    Listavailablevoiceconnectorregions. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Voice Connector Regions
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: chime-sdk-voice-voice-connector-regions
    baseUri: ''
    description: Amazon Web Services chime-sdk-voice — Voice Connector Regions business capability. Self-contained, no shared
      references.
    resources:
    - name: voice-connector-regions
      path: /voice-connector-regions
      operations:
      - name: amazonwebserviceslistavailablevoiceconnectorregions
        method: GET
        description: Amazon Web Services Listavailablevoiceconnectorregions
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: chime-sdk-voice-voice-connector-regions-rest
    port: 8080
    description: REST adapter for Amazon Web Services chime-sdk-voice — Voice Connector Regions. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/voice-connector-regions
      name: voice-connector-regions
      description: REST surface for voice-connector-regions.
      operations:
      - method: GET
        name: amazonwebserviceslistavailablevoiceconnectorregions
        description: Amazon Web Services Listavailablevoiceconnectorregions
        call: chime-sdk-voice-voice-connector-regions.amazonwebserviceslistavailablevoiceconnectorregions
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: chime-sdk-voice-voice-connector-regions-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services chime-sdk-voice — Voice Connector Regions. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-listavailablevoiceconnectorregions
      description: Amazon Web Services Listavailablevoiceconnectorregions
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: chime-sdk-voice-voice-connector-regions.amazonwebserviceslistavailablevoiceconnectorregions
      outputParameters:
      - type: object
        mapping: $.
