naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services mgn — UpdateWave
  description: 'Amazon Web Services mgn — UpdateWave. 1 operations. Lead operation: Amazon Web Services Updatewave. Self-contained
    Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - UpdateWave
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: mgn-updatewave
    baseUri: ''
    description: Amazon Web Services mgn — UpdateWave business capability. Self-contained, no shared references.
    resources:
    - name: UpdateWave
      path: /UpdateWave
      operations:
      - name: amazonwebservicesupdatewave
        method: POST
        description: Amazon Web Services Updatewave
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: mgn-updatewave-rest
    port: 8080
    description: REST adapter for Amazon Web Services mgn — UpdateWave. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/updatewave
      name: updatewave
      description: REST surface for UpdateWave.
      operations:
      - method: POST
        name: amazonwebservicesupdatewave
        description: Amazon Web Services Updatewave
        call: mgn-updatewave.amazonwebservicesupdatewave
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: mgn-updatewave-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services mgn — UpdateWave. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: amazon-web-services-updatewave
      description: Amazon Web Services Updatewave
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: mgn-updatewave.amazonwebservicesupdatewave
      outputParameters:
      - type: object
        mapping: $.
