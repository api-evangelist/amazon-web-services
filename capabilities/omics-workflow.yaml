naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services omics — Workflow
  description: 'Amazon Web Services omics — Workflow. 2 operations. Lead operation: Amazon Web Services Listworkflows. Self-contained
    Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Workflow
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: omics-workflow
    baseUri: ''
    description: Amazon Web Services omics — Workflow business capability. Self-contained, no shared references.
    resources:
    - name: workflow
      path: /workflow
      operations:
      - name: amazonwebserviceslistworkflows
        method: GET
        description: Amazon Web Services Listworkflows
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: workflow-id
      path: /workflow/{id}
      operations:
      - name: amazonwebservicesupdateworkflow
        method: POST
        description: Amazon Web Services Updateworkflow
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: omics-workflow-rest
    port: 8080
    description: REST adapter for Amazon Web Services omics — Workflow. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/workflow
      name: workflow
      description: REST surface for workflow.
      operations:
      - method: GET
        name: amazonwebserviceslistworkflows
        description: Amazon Web Services Listworkflows
        call: omics-workflow.amazonwebserviceslistworkflows
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/workflow/{id}
      name: workflow-id
      description: REST surface for workflow-id.
      operations:
      - method: POST
        name: amazonwebservicesupdateworkflow
        description: Amazon Web Services Updateworkflow
        call: omics-workflow.amazonwebservicesupdateworkflow
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: omics-workflow-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services omics — Workflow. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: amazon-web-services-listworkflows
      description: Amazon Web Services Listworkflows
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: omics-workflow.amazonwebserviceslistworkflows
      outputParameters:
      - type: object
        mapping: $.
    - name: amazon-web-services-updateworkflow
      description: Amazon Web Services Updateworkflow
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: omics-workflow.amazonwebservicesupdateworkflow
      outputParameters:
      - type: object
        mapping: $.
