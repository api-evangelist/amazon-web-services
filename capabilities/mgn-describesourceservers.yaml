naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services mgn — DescribeSourceServers
  description: 'Amazon Web Services mgn — DescribeSourceServers. 1 operations. Lead operation: Amazon Web Services Describesourceservers.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - DescribeSourceServers
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: mgn-describesourceservers
    baseUri: ''
    description: Amazon Web Services mgn — DescribeSourceServers business capability. Self-contained, no shared references.
    resources:
    - name: DescribeSourceServers
      path: /DescribeSourceServers
      operations:
      - name: amazonwebservicesdescribesourceservers
        method: POST
        description: Amazon Web Services Describesourceservers
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: mgn-describesourceservers-rest
    port: 8080
    description: REST adapter for Amazon Web Services mgn — DescribeSourceServers. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/describesourceservers
      name: describesourceservers
      description: REST surface for DescribeSourceServers.
      operations:
      - method: POST
        name: amazonwebservicesdescribesourceservers
        description: Amazon Web Services Describesourceservers
        call: mgn-describesourceservers.amazonwebservicesdescribesourceservers
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: mgn-describesourceservers-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services mgn — DescribeSourceServers. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-describesourceservers
      description: Amazon Web Services Describesourceservers
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: mgn-describesourceservers.amazonwebservicesdescribesourceservers
      outputParameters:
      - type: object
        mapping: $.
