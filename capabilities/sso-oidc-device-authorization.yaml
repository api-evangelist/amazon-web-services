naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services sso-oidc — Device Authorization
  description: 'Amazon Web Services sso-oidc — Device Authorization. 1 operations. Lead operation: Amazon Web Services Startdeviceauthorization.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Device Authorization
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: sso-oidc-device-authorization
    baseUri: ''
    description: Amazon Web Services sso-oidc — Device Authorization business capability. Self-contained, no shared references.
    resources:
    - name: device_authorization
      path: /device_authorization
      operations:
      - name: amazonwebservicesstartdeviceauthorization
        method: POST
        description: Amazon Web Services Startdeviceauthorization
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: sso-oidc-device-authorization-rest
    port: 8080
    description: REST adapter for Amazon Web Services sso-oidc — Device Authorization. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/device-authorization
      name: device-authorization
      description: REST surface for device_authorization.
      operations:
      - method: POST
        name: amazonwebservicesstartdeviceauthorization
        description: Amazon Web Services Startdeviceauthorization
        call: sso-oidc-device-authorization.amazonwebservicesstartdeviceauthorization
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: sso-oidc-device-authorization-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services sso-oidc — Device Authorization. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-startdeviceauthorization
      description: Amazon Web Services Startdeviceauthorization
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: sso-oidc-device-authorization.amazonwebservicesstartdeviceauthorization
      outputParameters:
      - type: object
        mapping: $.
