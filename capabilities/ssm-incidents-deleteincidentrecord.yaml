naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services ssm-incidents — deleteIncidentRecord
  description: 'Amazon Web Services ssm-incidents — deleteIncidentRecord. 1 operations. Lead operation: Amazon Web Services
    Deleteincidentrecord. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - deleteIncidentRecord
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: ssm-incidents-deleteincidentrecord
    baseUri: ''
    description: Amazon Web Services ssm-incidents — deleteIncidentRecord business capability. Self-contained, no shared references.
    resources:
    - name: deleteIncidentRecord
      path: /deleteIncidentRecord
      operations:
      - name: amazonwebservicesdeleteincidentrecord
        method: POST
        description: Amazon Web Services Deleteincidentrecord
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: ssm-incidents-deleteincidentrecord-rest
    port: 8080
    description: REST adapter for Amazon Web Services ssm-incidents — deleteIncidentRecord. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/deleteincidentrecord
      name: deleteincidentrecord
      description: REST surface for deleteIncidentRecord.
      operations:
      - method: POST
        name: amazonwebservicesdeleteincidentrecord
        description: Amazon Web Services Deleteincidentrecord
        call: ssm-incidents-deleteincidentrecord.amazonwebservicesdeleteincidentrecord
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ssm-incidents-deleteincidentrecord-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services ssm-incidents — deleteIncidentRecord. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-deleteincidentrecord
      description: Amazon Web Services Deleteincidentrecord
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ssm-incidents-deleteincidentrecord.amazonwebservicesdeleteincidentrecord
      outputParameters:
      - type: object
        mapping: $.
