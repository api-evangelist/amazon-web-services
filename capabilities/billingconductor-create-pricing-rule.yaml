naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services billingconductor — Create Pricing Rule
  description: 'Amazon Web Services billingconductor — Create Pricing Rule. 1 operations. Lead operation: Amazon Web Services
    Createpricingrule. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Create Pricing Rule
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: billingconductor-create-pricing-rule
    baseUri: ''
    description: Amazon Web Services billingconductor — Create Pricing Rule business capability. Self-contained, no shared
      references.
    resources:
    - name: create-pricing-rule
      path: /create-pricing-rule
      operations:
      - name: amazonwebservicescreatepricingrule
        method: POST
        description: Amazon Web Services Createpricingrule
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: billingconductor-create-pricing-rule-rest
    port: 8080
    description: REST adapter for Amazon Web Services billingconductor — Create Pricing Rule. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/create-pricing-rule
      name: create-pricing-rule
      description: REST surface for create-pricing-rule.
      operations:
      - method: POST
        name: amazonwebservicescreatepricingrule
        description: Amazon Web Services Createpricingrule
        call: billingconductor-create-pricing-rule.amazonwebservicescreatepricingrule
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: billingconductor-create-pricing-rule-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services billingconductor — Create Pricing Rule. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-createpricingrule
      description: Amazon Web Services Createpricingrule
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: billingconductor-create-pricing-rule.amazonwebservicescreatepricingrule
      outputParameters:
      - type: object
        mapping: $.
