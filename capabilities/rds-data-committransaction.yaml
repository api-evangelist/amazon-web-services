naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services rds-data — CommitTransaction
  description: 'Amazon Web Services rds-data — CommitTransaction. 1 operations. Lead operation: Amazon Web Services Committransaction.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - CommitTransaction
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: rds-data-committransaction
    baseUri: ''
    description: Amazon Web Services rds-data — CommitTransaction business capability. Self-contained, no shared references.
    resources:
    - name: CommitTransaction
      path: /CommitTransaction
      operations:
      - name: amazonwebservicescommittransaction
        method: POST
        description: Amazon Web Services Committransaction
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: rds-data-committransaction-rest
    port: 8080
    description: REST adapter for Amazon Web Services rds-data — CommitTransaction. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/committransaction
      name: committransaction
      description: REST surface for CommitTransaction.
      operations:
      - method: POST
        name: amazonwebservicescommittransaction
        description: Amazon Web Services Committransaction
        call: rds-data-committransaction.amazonwebservicescommittransaction
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: rds-data-committransaction-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services rds-data — CommitTransaction. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-committransaction
      description: Amazon Web Services Committransaction
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: rds-data-committransaction.amazonwebservicescommittransaction
      outputParameters:
      - type: object
        mapping: $.
