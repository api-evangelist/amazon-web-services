naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services drs — StartSourceNetworkReplication
  description: 'Amazon Web Services drs — StartSourceNetworkReplication. 1 operations. Lead operation: Amazon Web Services
    Startsourcenetworkreplication. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - StartSourceNetworkReplication
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: drs-startsourcenetworkreplication
    baseUri: ''
    description: Amazon Web Services drs — StartSourceNetworkReplication business capability. Self-contained, no shared references.
    resources:
    - name: StartSourceNetworkReplication
      path: /StartSourceNetworkReplication
      operations:
      - name: amazonwebservicesstartsourcenetworkreplication
        method: POST
        description: Amazon Web Services Startsourcenetworkreplication
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: drs-startsourcenetworkreplication-rest
    port: 8080
    description: REST adapter for Amazon Web Services drs — StartSourceNetworkReplication. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/startsourcenetworkreplication
      name: startsourcenetworkreplication
      description: REST surface for StartSourceNetworkReplication.
      operations:
      - method: POST
        name: amazonwebservicesstartsourcenetworkreplication
        description: Amazon Web Services Startsourcenetworkreplication
        call: drs-startsourcenetworkreplication.amazonwebservicesstartsourcenetworkreplication
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: drs-startsourcenetworkreplication-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services drs — StartSourceNetworkReplication. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-startsourcenetworkreplication
      description: Amazon Web Services Startsourcenetworkreplication
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: drs-startsourcenetworkreplication.amazonwebservicesstartsourcenetworkreplication
      outputParameters:
      - type: object
        mapping: $.
