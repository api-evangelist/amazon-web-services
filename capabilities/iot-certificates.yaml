naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services iot — Certificates
  description: 'Amazon Web Services iot — Certificates. 2 operations. Lead operation: Amazon Web Services Listcertificates.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Certificates
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: iot-certificates
    baseUri: ''
    description: Amazon Web Services iot — Certificates business capability. Self-contained, no shared references.
    resources:
    - name: certificates
      path: /certificates
      operations:
      - name: amazonwebserviceslistcertificates
        method: GET
        description: Amazon Web Services Listcertificates
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: certificates-certificateId
      path: /certificates/{certificateId}
      operations:
      - name: amazonwebservicesupdatecertificate
        method: PUT
        description: Amazon Web Services Updatecertificate
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: iot-certificates-rest
    port: 8080
    description: REST adapter for Amazon Web Services iot — Certificates. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/certificates
      name: certificates
      description: REST surface for certificates.
      operations:
      - method: GET
        name: amazonwebserviceslistcertificates
        description: Amazon Web Services Listcertificates
        call: iot-certificates.amazonwebserviceslistcertificates
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/certificates/{certificateid}
      name: certificates-certificateid
      description: REST surface for certificates-certificateId.
      operations:
      - method: PUT
        name: amazonwebservicesupdatecertificate
        description: Amazon Web Services Updatecertificate
        call: iot-certificates.amazonwebservicesupdatecertificate
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: iot-certificates-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services iot — Certificates. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: amazon-web-services-listcertificates
      description: Amazon Web Services Listcertificates
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: iot-certificates.amazonwebserviceslistcertificates
      outputParameters:
      - type: object
        mapping: $.
    - name: amazon-web-services-updatecertificate
      description: Amazon Web Services Updatecertificate
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: iot-certificates.amazonwebservicesupdatecertificate
      outputParameters:
      - type: object
        mapping: $.
