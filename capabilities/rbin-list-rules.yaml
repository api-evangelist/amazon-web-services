naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services rbin — List Rules
  description: 'Amazon Web Services rbin — List Rules. 1 operations. Lead operation: Amazon Web Services Listrules. Self-contained
    Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - List Rules
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: rbin-list-rules
    baseUri: ''
    description: Amazon Web Services rbin — List Rules business capability. Self-contained, no shared references.
    resources:
    - name: list-rules
      path: /list-rules
      operations:
      - name: amazonwebserviceslistrules
        method: POST
        description: Amazon Web Services Listrules
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: rbin-list-rules-rest
    port: 8080
    description: REST adapter for Amazon Web Services rbin — List Rules. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/list-rules
      name: list-rules
      description: REST surface for list-rules.
      operations:
      - method: POST
        name: amazonwebserviceslistrules
        description: Amazon Web Services Listrules
        call: rbin-list-rules.amazonwebserviceslistrules
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: rbin-list-rules-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services rbin — List Rules. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: amazon-web-services-listrules
      description: Amazon Web Services Listrules
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: rbin-list-rules.amazonwebserviceslistrules
      outputParameters:
      - type: object
        mapping: $.
