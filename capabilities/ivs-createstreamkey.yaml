naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services ivs — CreateStreamKey
  description: 'Amazon Web Services ivs — CreateStreamKey. 1 operations. Lead operation: Amazon Web Services Createstreamkey.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - CreateStreamKey
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: ivs-createstreamkey
    baseUri: ''
    description: Amazon Web Services ivs — CreateStreamKey business capability. Self-contained, no shared references.
    resources:
    - name: CreateStreamKey
      path: /CreateStreamKey
      operations:
      - name: amazonwebservicescreatestreamkey
        method: POST
        description: Amazon Web Services Createstreamkey
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: ivs-createstreamkey-rest
    port: 8080
    description: REST adapter for Amazon Web Services ivs — CreateStreamKey. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/createstreamkey
      name: createstreamkey
      description: REST surface for CreateStreamKey.
      operations:
      - method: POST
        name: amazonwebservicescreatestreamkey
        description: Amazon Web Services Createstreamkey
        call: ivs-createstreamkey.amazonwebservicescreatestreamkey
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ivs-createstreamkey-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services ivs — CreateStreamKey. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: amazon-web-services-createstreamkey
      description: Amazon Web Services Createstreamkey
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ivs-createstreamkey.amazonwebservicescreatestreamkey
      outputParameters:
      - type: object
        mapping: $.
