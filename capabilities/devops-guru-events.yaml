naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services devops-guru — Events
  description: 'Amazon Web Services devops-guru — Events. 1 operations. Lead operation: Amazon Web Services Listevents. Self-contained
    Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Events
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: devops-guru-events
    baseUri: ''
    description: Amazon Web Services devops-guru — Events business capability. Self-contained, no shared references.
    resources:
    - name: events
      path: /events
      operations:
      - name: amazonwebserviceslistevents
        method: POST
        description: Amazon Web Services Listevents
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: devops-guru-events-rest
    port: 8080
    description: REST adapter for Amazon Web Services devops-guru — Events. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/events
      name: events
      description: REST surface for events.
      operations:
      - method: POST
        name: amazonwebserviceslistevents
        description: Amazon Web Services Listevents
        call: devops-guru-events.amazonwebserviceslistevents
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: devops-guru-events-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services devops-guru — Events. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: amazon-web-services-listevents
      description: Amazon Web Services Listevents
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: devops-guru-events.amazonwebserviceslistevents
      outputParameters:
      - type: object
        mapping: $.
