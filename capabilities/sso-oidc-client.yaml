naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services sso-oidc — Client
  description: 'Amazon Web Services sso-oidc — Client. 1 operations. Lead operation: Amazon Web Services Registerclient. Self-contained
    Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Client
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: sso-oidc-client
    baseUri: ''
    description: Amazon Web Services sso-oidc — Client business capability. Self-contained, no shared references.
    resources:
    - name: client-register
      path: /client/register
      operations:
      - name: amazonwebservicesregisterclient
        method: POST
        description: Amazon Web Services Registerclient
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: sso-oidc-client-rest
    port: 8080
    description: REST adapter for Amazon Web Services sso-oidc — Client. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/client/register
      name: client-register
      description: REST surface for client-register.
      operations:
      - method: POST
        name: amazonwebservicesregisterclient
        description: Amazon Web Services Registerclient
        call: sso-oidc-client.amazonwebservicesregisterclient
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: sso-oidc-client-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services sso-oidc — Client. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: amazon-web-services-registerclient
      description: Amazon Web Services Registerclient
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: sso-oidc-client.amazonwebservicesregisterclient
      outputParameters:
      - type: object
        mapping: $.
