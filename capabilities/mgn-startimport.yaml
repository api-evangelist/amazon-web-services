naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services mgn — StartImport
  description: 'Amazon Web Services mgn — StartImport. 1 operations. Lead operation: Amazon Web Services Startimport. Self-contained
    Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - StartImport
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: mgn-startimport
    baseUri: ''
    description: Amazon Web Services mgn — StartImport business capability. Self-contained, no shared references.
    resources:
    - name: StartImport
      path: /StartImport
      operations:
      - name: amazonwebservicesstartimport
        method: POST
        description: Amazon Web Services Startimport
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: mgn-startimport-rest
    port: 8080
    description: REST adapter for Amazon Web Services mgn — StartImport. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/startimport
      name: startimport
      description: REST surface for StartImport.
      operations:
      - method: POST
        name: amazonwebservicesstartimport
        description: Amazon Web Services Startimport
        call: mgn-startimport.amazonwebservicesstartimport
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: mgn-startimport-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services mgn — StartImport. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: amazon-web-services-startimport
      description: Amazon Web Services Startimport
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: mgn-startimport.amazonwebservicesstartimport
      outputParameters:
      - type: object
        mapping: $.
