naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services AWS S3 — Bucket Analytics Configuration
  description: 'Amazon Web Services AWS S3 — Bucket Analytics Configuration. 1 operations. Lead operation: Amazon Web Services
    Put Bucket Analytics Configuration. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Bucket Analytics Configuration
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: s3-bucket-analytics-configuration
    baseUri: ''
    description: Amazon Web Services AWS S3 — Bucket Analytics Configuration business capability. Self-contained, no shared
      references.
    resources:
    - name: Bucket}?analytics
      path: /{Bucket}?analytics
      operations:
      - name: putbucketanalyticsconfiguration
        method: PUT
        description: Amazon Web Services Put Bucket Analytics Configuration
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: s3-bucket-analytics-configuration-rest
    port: 8080
    description: REST adapter for Amazon Web Services AWS S3 — Bucket Analytics Configuration. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/bucket-analytics
      name: bucket-analytics
      description: REST surface for Bucket}?analytics.
      operations:
      - method: PUT
        name: putbucketanalyticsconfiguration
        description: Amazon Web Services Put Bucket Analytics Configuration
        call: s3-bucket-analytics-configuration.putbucketanalyticsconfiguration
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: s3-bucket-analytics-configuration-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services AWS S3 — Bucket Analytics Configuration. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-put-bucket
      description: Amazon Web Services Put Bucket Analytics Configuration
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: s3-bucket-analytics-configuration.putbucketanalyticsconfiguration
      outputParameters:
      - type: object
        mapping: $.
