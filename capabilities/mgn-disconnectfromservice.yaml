naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services mgn — DisconnectFromService
  description: 'Amazon Web Services mgn — DisconnectFromService. 1 operations. Lead operation: Amazon Web Services Disconnectfromservice.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - DisconnectFromService
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: mgn-disconnectfromservice
    baseUri: ''
    description: Amazon Web Services mgn — DisconnectFromService business capability. Self-contained, no shared references.
    resources:
    - name: DisconnectFromService
      path: /DisconnectFromService
      operations:
      - name: amazonwebservicesdisconnectfromservice
        method: POST
        description: Amazon Web Services Disconnectfromservice
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: mgn-disconnectfromservice-rest
    port: 8080
    description: REST adapter for Amazon Web Services mgn — DisconnectFromService. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/disconnectfromservice
      name: disconnectfromservice
      description: REST surface for DisconnectFromService.
      operations:
      - method: POST
        name: amazonwebservicesdisconnectfromservice
        description: Amazon Web Services Disconnectfromservice
        call: mgn-disconnectfromservice.amazonwebservicesdisconnectfromservice
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: mgn-disconnectfromservice-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services mgn — DisconnectFromService. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-disconnectfromservice
      description: Amazon Web Services Disconnectfromservice
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: mgn-disconnectfromservice.amazonwebservicesdisconnectfromservice
      outputParameters:
      - type: object
        mapping: $.
