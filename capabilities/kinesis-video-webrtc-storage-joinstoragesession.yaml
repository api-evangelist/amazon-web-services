naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services kinesis-video-webrtc-storage — joinStorageSession
  description: 'Amazon Web Services kinesis-video-webrtc-storage — joinStorageSession. 1 operations. Lead operation: Amazon
    Web Services Joinstoragesession. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - joinStorageSession
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: kinesis-video-webrtc-storage-joinstoragesession
    baseUri: ''
    description: Amazon Web Services kinesis-video-webrtc-storage — joinStorageSession business capability. Self-contained,
      no shared references.
    resources:
    - name: joinStorageSession
      path: /joinStorageSession
      operations:
      - name: amazonwebservicesjoinstoragesession
        method: POST
        description: Amazon Web Services Joinstoragesession
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: kinesis-video-webrtc-storage-joinstoragesession-rest
    port: 8080
    description: REST adapter for Amazon Web Services kinesis-video-webrtc-storage — joinStorageSession. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/joinstoragesession
      name: joinstoragesession
      description: REST surface for joinStorageSession.
      operations:
      - method: POST
        name: amazonwebservicesjoinstoragesession
        description: Amazon Web Services Joinstoragesession
        call: kinesis-video-webrtc-storage-joinstoragesession.amazonwebservicesjoinstoragesession
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: kinesis-video-webrtc-storage-joinstoragesession-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services kinesis-video-webrtc-storage — joinStorageSession. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-joinstoragesession
      description: Amazon Web Services Joinstoragesession
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: kinesis-video-webrtc-storage-joinstoragesession.amazonwebservicesjoinstoragesession
      outputParameters:
      - type: object
        mapping: $.
