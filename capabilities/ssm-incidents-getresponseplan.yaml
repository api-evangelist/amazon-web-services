naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services ssm-incidents — getResponsePlan
  description: 'Amazon Web Services ssm-incidents — getResponsePlan. 1 operations. Lead operation: Amazon Web Services Getresponseplan.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - getResponsePlan
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: ssm-incidents-getresponseplan
    baseUri: ''
    description: Amazon Web Services ssm-incidents — getResponsePlan business capability. Self-contained, no shared references.
    resources:
    - name: getResponsePlan
      path: /getResponsePlan
      operations:
      - name: amazonwebservicesgetresponseplan
        method: GET
        description: Amazon Web Services Getresponseplan
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: ssm-incidents-getresponseplan-rest
    port: 8080
    description: REST adapter for Amazon Web Services ssm-incidents — getResponsePlan. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/getresponseplan
      name: getresponseplan
      description: REST surface for getResponsePlan.
      operations:
      - method: GET
        name: amazonwebservicesgetresponseplan
        description: Amazon Web Services Getresponseplan
        call: ssm-incidents-getresponseplan.amazonwebservicesgetresponseplan
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ssm-incidents-getresponseplan-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services ssm-incidents — getResponsePlan. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-getresponseplan
      description: Amazon Web Services Getresponseplan
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ssm-incidents-getresponseplan.amazonwebservicesgetresponseplan
      outputParameters:
      - type: object
        mapping: $.
