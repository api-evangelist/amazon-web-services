naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services marketplace-catalog — DescribeChangeSet
  description: 'Amazon Web Services marketplace-catalog — DescribeChangeSet. 1 operations. Lead operation: Amazon Web Services
    Describechangeset. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - DescribeChangeSet
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: marketplace-catalog-describechangeset
    baseUri: ''
    description: Amazon Web Services marketplace-catalog — DescribeChangeSet business capability. Self-contained, no shared
      references.
    resources:
    - name: DescribeChangeSet
      path: /DescribeChangeSet
      operations:
      - name: amazonwebservicesdescribechangeset
        method: GET
        description: Amazon Web Services Describechangeset
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: marketplace-catalog-describechangeset-rest
    port: 8080
    description: REST adapter for Amazon Web Services marketplace-catalog — DescribeChangeSet. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/describechangeset
      name: describechangeset
      description: REST surface for DescribeChangeSet.
      operations:
      - method: GET
        name: amazonwebservicesdescribechangeset
        description: Amazon Web Services Describechangeset
        call: marketplace-catalog-describechangeset.amazonwebservicesdescribechangeset
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: marketplace-catalog-describechangeset-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services marketplace-catalog — DescribeChangeSet. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-describechangeset
      description: Amazon Web Services Describechangeset
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: marketplace-catalog-describechangeset.amazonwebservicesdescribechangeset
      outputParameters:
      - type: object
        mapping: $.
