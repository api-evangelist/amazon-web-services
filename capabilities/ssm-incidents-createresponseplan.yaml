naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services ssm-incidents — createResponsePlan
  description: 'Amazon Web Services ssm-incidents — createResponsePlan. 1 operations. Lead operation: Amazon Web Services
    Createresponseplan. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - createResponsePlan
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: ssm-incidents-createresponseplan
    baseUri: ''
    description: Amazon Web Services ssm-incidents — createResponsePlan business capability. Self-contained, no shared references.
    resources:
    - name: createResponsePlan
      path: /createResponsePlan
      operations:
      - name: amazonwebservicescreateresponseplan
        method: POST
        description: Amazon Web Services Createresponseplan
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: ssm-incidents-createresponseplan-rest
    port: 8080
    description: REST adapter for Amazon Web Services ssm-incidents — createResponsePlan. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/createresponseplan
      name: createresponseplan
      description: REST surface for createResponsePlan.
      operations:
      - method: POST
        name: amazonwebservicescreateresponseplan
        description: Amazon Web Services Createresponseplan
        call: ssm-incidents-createresponseplan.amazonwebservicescreateresponseplan
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ssm-incidents-createresponseplan-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services ssm-incidents — createResponsePlan. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-createresponseplan
      description: Amazon Web Services Createresponseplan
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ssm-incidents-createresponseplan.amazonwebservicescreateresponseplan
      outputParameters:
      - type: object
        mapping: $.
