naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services amplifybackend — s3Buckets
  description: 'Amazon Web Services amplifybackend — s3Buckets. 1 operations. Lead operation: Amazon Web Services Lists3buckets.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - s3Buckets
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: amplifybackend-s3buckets
    baseUri: ''
    description: Amazon Web Services amplifybackend — s3Buckets business capability. Self-contained, no shared references.
    resources:
    - name: s3Buckets
      path: /s3Buckets
      operations:
      - name: amazonwebserviceslists3buckets
        method: POST
        description: Amazon Web Services Lists3buckets
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: amplifybackend-s3buckets-rest
    port: 8080
    description: REST adapter for Amazon Web Services amplifybackend — s3Buckets. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/s3buckets
      name: s3buckets
      description: REST surface for s3Buckets.
      operations:
      - method: POST
        name: amazonwebserviceslists3buckets
        description: Amazon Web Services Lists3buckets
        call: amplifybackend-s3buckets.amazonwebserviceslists3buckets
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: amplifybackend-s3buckets-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services amplifybackend — s3Buckets. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: amazon-web-services-lists3buckets
      description: Amazon Web Services Lists3buckets
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: amplifybackend-s3buckets.amazonwebserviceslists3buckets
      outputParameters:
      - type: object
        mapping: $.
