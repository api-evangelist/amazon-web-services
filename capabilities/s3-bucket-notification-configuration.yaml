naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services AWS S3 — Bucket Notification Configuration
  description: 'Amazon Web Services AWS S3 — Bucket Notification Configuration. 1 operations. Lead operation: Amazon Web Services
    Put Bucket Notification Configuration. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Bucket Notification Configuration
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: s3-bucket-notification-configuration
    baseUri: ''
    description: Amazon Web Services AWS S3 — Bucket Notification Configuration business capability. Self-contained, no shared
      references.
    resources:
    - name: Bucket}?notification
      path: /{Bucket}?notification
      operations:
      - name: putbucketnotificationconfiguration
        method: PUT
        description: Amazon Web Services Put Bucket Notification Configuration
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: s3-bucket-notification-configuration-rest
    port: 8080
    description: REST adapter for Amazon Web Services AWS S3 — Bucket Notification Configuration. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/bucket-notification
      name: bucket-notification
      description: REST surface for Bucket}?notification.
      operations:
      - method: PUT
        name: putbucketnotificationconfiguration
        description: Amazon Web Services Put Bucket Notification Configuration
        call: s3-bucket-notification-configuration.putbucketnotificationconfiguration
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: s3-bucket-notification-configuration-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services AWS S3 — Bucket Notification Configuration. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-put-bucket
      description: Amazon Web Services Put Bucket Notification Configuration
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: s3-bucket-notification-configuration.putbucketnotificationconfiguration
      outputParameters:
      - type: object
        mapping: $.
