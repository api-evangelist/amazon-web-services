naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services rds-data — RollbackTransaction
  description: 'Amazon Web Services rds-data — RollbackTransaction. 1 operations. Lead operation: Amazon Web Services Rollbacktransaction.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - RollbackTransaction
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: rds-data-rollbacktransaction
    baseUri: ''
    description: Amazon Web Services rds-data — RollbackTransaction business capability. Self-contained, no shared references.
    resources:
    - name: RollbackTransaction
      path: /RollbackTransaction
      operations:
      - name: amazonwebservicesrollbacktransaction
        method: POST
        description: Amazon Web Services Rollbacktransaction
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: rds-data-rollbacktransaction-rest
    port: 8080
    description: REST adapter for Amazon Web Services rds-data — RollbackTransaction. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/rollbacktransaction
      name: rollbacktransaction
      description: REST surface for RollbackTransaction.
      operations:
      - method: POST
        name: amazonwebservicesrollbacktransaction
        description: Amazon Web Services Rollbacktransaction
        call: rds-data-rollbacktransaction.amazonwebservicesrollbacktransaction
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: rds-data-rollbacktransaction-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services rds-data — RollbackTransaction. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-rollbacktransaction
      description: Amazon Web Services Rollbacktransaction
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: rds-data-rollbacktransaction.amazonwebservicesrollbacktransaction
      outputParameters:
      - type: object
        mapping: $.
