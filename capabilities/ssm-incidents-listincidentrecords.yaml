naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services ssm-incidents — listIncidentRecords
  description: 'Amazon Web Services ssm-incidents — listIncidentRecords. 1 operations. Lead operation: Amazon Web Services
    Listincidentrecords. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - listIncidentRecords
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: ssm-incidents-listincidentrecords
    baseUri: ''
    description: Amazon Web Services ssm-incidents — listIncidentRecords business capability. Self-contained, no shared references.
    resources:
    - name: listIncidentRecords
      path: /listIncidentRecords
      operations:
      - name: amazonwebserviceslistincidentrecords
        method: POST
        description: Amazon Web Services Listincidentrecords
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: ssm-incidents-listincidentrecords-rest
    port: 8080
    description: REST adapter for Amazon Web Services ssm-incidents — listIncidentRecords. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/listincidentrecords
      name: listincidentrecords
      description: REST surface for listIncidentRecords.
      operations:
      - method: POST
        name: amazonwebserviceslistincidentrecords
        description: Amazon Web Services Listincidentrecords
        call: ssm-incidents-listincidentrecords.amazonwebserviceslistincidentrecords
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ssm-incidents-listincidentrecords-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services ssm-incidents — listIncidentRecords. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-listincidentrecords
      description: Amazon Web Services Listincidentrecords
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ssm-incidents-listincidentrecords.amazonwebserviceslistincidentrecords
      outputParameters:
      - type: object
        mapping: $.
