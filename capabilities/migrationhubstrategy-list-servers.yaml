naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services migrationhubstrategy — List Servers
  description: 'Amazon Web Services migrationhubstrategy — List Servers. 1 operations. Lead operation: Amazon Web Services
    Listservers. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - List Servers
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: migrationhubstrategy-list-servers
    baseUri: ''
    description: Amazon Web Services migrationhubstrategy — List Servers business capability. Self-contained, no shared references.
    resources:
    - name: list-servers
      path: /list-servers
      operations:
      - name: amazonwebserviceslistservers
        method: POST
        description: Amazon Web Services Listservers
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: migrationhubstrategy-list-servers-rest
    port: 8080
    description: REST adapter for Amazon Web Services migrationhubstrategy — List Servers. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/list-servers
      name: list-servers
      description: REST surface for list-servers.
      operations:
      - method: POST
        name: amazonwebserviceslistservers
        description: Amazon Web Services Listservers
        call: migrationhubstrategy-list-servers.amazonwebserviceslistservers
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: migrationhubstrategy-list-servers-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services migrationhubstrategy — List Servers. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-listservers
      description: Amazon Web Services Listservers
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: migrationhubstrategy-list-servers.amazonwebserviceslistservers
      outputParameters:
      - type: object
        mapping: $.
