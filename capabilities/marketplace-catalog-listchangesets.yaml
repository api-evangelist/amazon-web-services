naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services marketplace-catalog — ListChangeSets
  description: 'Amazon Web Services marketplace-catalog — ListChangeSets. 1 operations. Lead operation: Amazon Web Services
    Listchangesets. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - ListChangeSets
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: marketplace-catalog-listchangesets
    baseUri: ''
    description: Amazon Web Services marketplace-catalog — ListChangeSets business capability. Self-contained, no shared references.
    resources:
    - name: ListChangeSets
      path: /ListChangeSets
      operations:
      - name: amazonwebserviceslistchangesets
        method: POST
        description: Amazon Web Services Listchangesets
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: marketplace-catalog-listchangesets-rest
    port: 8080
    description: REST adapter for Amazon Web Services marketplace-catalog — ListChangeSets. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/listchangesets
      name: listchangesets
      description: REST surface for ListChangeSets.
      operations:
      - method: POST
        name: amazonwebserviceslistchangesets
        description: Amazon Web Services Listchangesets
        call: marketplace-catalog-listchangesets.amazonwebserviceslistchangesets
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: marketplace-catalog-listchangesets-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services marketplace-catalog — ListChangeSets. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-listchangesets
      description: Amazon Web Services Listchangesets
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: marketplace-catalog-listchangesets.amazonwebserviceslistchangesets
      outputParameters:
      - type: object
        mapping: $.
