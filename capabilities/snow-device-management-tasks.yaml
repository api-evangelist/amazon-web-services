naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services snow-device-management — Tasks
  description: 'Amazon Web Services snow-device-management — Tasks. 1 operations. Lead operation: Amazon Web Services Listtasks.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Tasks
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: snow-device-management-tasks
    baseUri: ''
    description: Amazon Web Services snow-device-management — Tasks business capability. Self-contained, no shared references.
    resources:
    - name: tasks
      path: /tasks
      operations:
      - name: amazonwebserviceslisttasks
        method: GET
        description: Amazon Web Services Listtasks
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: snow-device-management-tasks-rest
    port: 8080
    description: REST adapter for Amazon Web Services snow-device-management — Tasks. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/tasks
      name: tasks
      description: REST surface for tasks.
      operations:
      - method: GET
        name: amazonwebserviceslisttasks
        description: Amazon Web Services Listtasks
        call: snow-device-management-tasks.amazonwebserviceslisttasks
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: snow-device-management-tasks-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services snow-device-management — Tasks. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-listtasks
      description: Amazon Web Services Listtasks
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: snow-device-management-tasks.amazonwebserviceslisttasks
      outputParameters:
      - type: object
        mapping: $.
