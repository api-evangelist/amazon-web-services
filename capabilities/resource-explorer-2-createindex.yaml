naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services resource-explorer-2 — CreateIndex
  description: 'Amazon Web Services resource-explorer-2 — CreateIndex. 1 operations. Lead operation: Amazon Web Services Createindex.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - CreateIndex
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: resource-explorer-2-createindex
    baseUri: ''
    description: Amazon Web Services resource-explorer-2 — CreateIndex business capability. Self-contained, no shared references.
    resources:
    - name: CreateIndex
      path: /CreateIndex
      operations:
      - name: amazonwebservicescreateindex
        method: POST
        description: Amazon Web Services Createindex
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: resource-explorer-2-createindex-rest
    port: 8080
    description: REST adapter for Amazon Web Services resource-explorer-2 — CreateIndex. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/createindex
      name: createindex
      description: REST surface for CreateIndex.
      operations:
      - method: POST
        name: amazonwebservicescreateindex
        description: Amazon Web Services Createindex
        call: resource-explorer-2-createindex.amazonwebservicescreateindex
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: resource-explorer-2-createindex-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services resource-explorer-2 — CreateIndex. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-createindex
      description: Amazon Web Services Createindex
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: resource-explorer-2-createindex.amazonwebservicescreateindex
      outputParameters:
      - type: object
        mapping: $.
