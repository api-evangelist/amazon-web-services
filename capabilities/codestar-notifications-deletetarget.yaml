naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services codestar-notifications — deleteTarget
  description: 'Amazon Web Services codestar-notifications — deleteTarget. 1 operations. Lead operation: Amazon Web Services
    Deletetarget. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - deleteTarget
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: codestar-notifications-deletetarget
    baseUri: ''
    description: Amazon Web Services codestar-notifications — deleteTarget business capability. Self-contained, no shared
      references.
    resources:
    - name: deleteTarget
      path: /deleteTarget
      operations:
      - name: amazonwebservicesdeletetarget
        method: POST
        description: Amazon Web Services Deletetarget
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: codestar-notifications-deletetarget-rest
    port: 8080
    description: REST adapter for Amazon Web Services codestar-notifications — deleteTarget. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/deletetarget
      name: deletetarget
      description: REST surface for deleteTarget.
      operations:
      - method: POST
        name: amazonwebservicesdeletetarget
        description: Amazon Web Services Deletetarget
        call: codestar-notifications-deletetarget.amazonwebservicesdeletetarget
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: codestar-notifications-deletetarget-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services codestar-notifications — deleteTarget. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-deletetarget
      description: Amazon Web Services Deletetarget
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: codestar-notifications-deletetarget.amazonwebservicesdeletetarget
      outputParameters:
      - type: object
        mapping: $.
