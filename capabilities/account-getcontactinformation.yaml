naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services account — getContactInformation
  description: 'Amazon Web Services account — getContactInformation. 1 operations. Lead operation: Amazon Web Services Getcontactinformation.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - getContactInformation
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: account-getcontactinformation
    baseUri: ''
    description: Amazon Web Services account — getContactInformation business capability. Self-contained, no shared references.
    resources:
    - name: getContactInformation
      path: /getContactInformation
      operations:
      - name: amazonwebservicesgetcontactinformation
        method: POST
        description: Amazon Web Services Getcontactinformation
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: account-getcontactinformation-rest
    port: 8080
    description: REST adapter for Amazon Web Services account — getContactInformation. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/getcontactinformation
      name: getcontactinformation
      description: REST surface for getContactInformation.
      operations:
      - method: POST
        name: amazonwebservicesgetcontactinformation
        description: Amazon Web Services Getcontactinformation
        call: account-getcontactinformation.amazonwebservicesgetcontactinformation
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: account-getcontactinformation-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services account — getContactInformation. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-getcontactinformation
      description: Amazon Web Services Getcontactinformation
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: account-getcontactinformation.amazonwebservicesgetcontactinformation
      outputParameters:
      - type: object
        mapping: $.
