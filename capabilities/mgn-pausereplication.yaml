naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services mgn — PauseReplication
  description: 'Amazon Web Services mgn — PauseReplication. 1 operations. Lead operation: Amazon Web Services Pausereplication.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - PauseReplication
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: mgn-pausereplication
    baseUri: ''
    description: Amazon Web Services mgn — PauseReplication business capability. Self-contained, no shared references.
    resources:
    - name: PauseReplication
      path: /PauseReplication
      operations:
      - name: amazonwebservicespausereplication
        method: POST
        description: Amazon Web Services Pausereplication
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: mgn-pausereplication-rest
    port: 8080
    description: REST adapter for Amazon Web Services mgn — PauseReplication. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/pausereplication
      name: pausereplication
      description: REST surface for PauseReplication.
      operations:
      - method: POST
        name: amazonwebservicespausereplication
        description: Amazon Web Services Pausereplication
        call: mgn-pausereplication.amazonwebservicespausereplication
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: mgn-pausereplication-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services mgn — PauseReplication. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: amazon-web-services-pausereplication
      description: Amazon Web Services Pausereplication
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: mgn-pausereplication.amazonwebservicespausereplication
      outputParameters:
      - type: object
        mapping: $.
