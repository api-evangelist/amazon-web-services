naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services mgn — ListConnectors
  description: 'Amazon Web Services mgn — ListConnectors. 1 operations. Lead operation: Amazon Web Services Listconnectors.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - ListConnectors
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: mgn-listconnectors
    baseUri: ''
    description: Amazon Web Services mgn — ListConnectors business capability. Self-contained, no shared references.
    resources:
    - name: ListConnectors
      path: /ListConnectors
      operations:
      - name: amazonwebserviceslistconnectors
        method: POST
        description: Amazon Web Services Listconnectors
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: mgn-listconnectors-rest
    port: 8080
    description: REST adapter for Amazon Web Services mgn — ListConnectors. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/listconnectors
      name: listconnectors
      description: REST surface for ListConnectors.
      operations:
      - method: POST
        name: amazonwebserviceslistconnectors
        description: Amazon Web Services Listconnectors
        call: mgn-listconnectors.amazonwebserviceslistconnectors
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: mgn-listconnectors-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services mgn — ListConnectors. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: amazon-web-services-listconnectors
      description: Amazon Web Services Listconnectors
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: mgn-listconnectors.amazonwebserviceslistconnectors
      outputParameters:
      - type: object
        mapping: $.
