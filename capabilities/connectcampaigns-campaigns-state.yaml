naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services connectcampaigns — Campaigns State
  description: 'Amazon Web Services connectcampaigns — Campaigns State. 1 operations. Lead operation: Amazon Web Services
    Getcampaignstatebatch. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Campaigns State
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: connectcampaigns-campaigns-state
    baseUri: ''
    description: Amazon Web Services connectcampaigns — Campaigns State business capability. Self-contained, no shared references.
    resources:
    - name: campaigns-state
      path: /campaigns-state
      operations:
      - name: amazonwebservicesgetcampaignstatebatch
        method: POST
        description: Amazon Web Services Getcampaignstatebatch
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: connectcampaigns-campaigns-state-rest
    port: 8080
    description: REST adapter for Amazon Web Services connectcampaigns — Campaigns State. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/campaigns-state
      name: campaigns-state
      description: REST surface for campaigns-state.
      operations:
      - method: POST
        name: amazonwebservicesgetcampaignstatebatch
        description: Amazon Web Services Getcampaignstatebatch
        call: connectcampaigns-campaigns-state.amazonwebservicesgetcampaignstatebatch
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: connectcampaigns-campaigns-state-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services connectcampaigns — Campaigns State. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-getcampaignstatebatch
      description: Amazon Web Services Getcampaignstatebatch
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: connectcampaigns-campaigns-state.amazonwebservicesgetcampaignstatebatch
      outputParameters:
      - type: object
        mapping: $.
