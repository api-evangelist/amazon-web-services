naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services imagebuilder — DeleteDistributionConfiguration
  description: 'Amazon Web Services imagebuilder — DeleteDistributionConfiguration. 1 operations. Lead operation: Amazon Web
    Services Deletedistributionconfiguration. Self-contained Naftiko capability covering one Amazon Web Services business
    surface.'
  tags:
  - Amazon Web Services
  - DeleteDistributionConfiguration
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: imagebuilder-deletedistributionconfiguration
    baseUri: ''
    description: Amazon Web Services imagebuilder — DeleteDistributionConfiguration business capability. Self-contained, no
      shared references.
    resources:
    - name: DeleteDistributionConfiguration
      path: /DeleteDistributionConfiguration
      operations:
      - name: amazonwebservicesdeletedistributionconfiguration
        method: DELETE
        description: Amazon Web Services Deletedistributionconfiguration
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: imagebuilder-deletedistributionconfiguration-rest
    port: 8080
    description: REST adapter for Amazon Web Services imagebuilder — DeleteDistributionConfiguration. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/deletedistributionconfiguration
      name: deletedistributionconfiguration
      description: REST surface for DeleteDistributionConfiguration.
      operations:
      - method: DELETE
        name: amazonwebservicesdeletedistributionconfiguration
        description: Amazon Web Services Deletedistributionconfiguration
        call: imagebuilder-deletedistributionconfiguration.amazonwebservicesdeletedistributionconfiguration
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: imagebuilder-deletedistributionconfiguration-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services imagebuilder — DeleteDistributionConfiguration. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-deletedistributionconfiguration
      description: Amazon Web Services Deletedistributionconfiguration
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: imagebuilder-deletedistributionconfiguration.amazonwebservicesdeletedistributionconfiguration
      outputParameters:
      - type: object
        mapping: $.
