naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services lakeformation — GetTemporaryGluePartitionCredentials
  description: 'Amazon Web Services lakeformation — GetTemporaryGluePartitionCredentials. 1 operations. Lead operation: Amazon
    Web Services Gettemporarygluepartitioncredentials. Self-contained Naftiko capability covering one Amazon Web Services
    business surface.'
  tags:
  - Amazon Web Services
  - GetTemporaryGluePartitionCredentials
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: lakeformation-gettemporarygluepartitioncredentials
    baseUri: ''
    description: Amazon Web Services lakeformation — GetTemporaryGluePartitionCredentials business capability. Self-contained,
      no shared references.
    resources:
    - name: GetTemporaryGluePartitionCredentials
      path: /GetTemporaryGluePartitionCredentials
      operations:
      - name: amazonwebservicesgettemporarygluepartitioncredentials
        method: POST
        description: Amazon Web Services Gettemporarygluepartitioncredentials
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: lakeformation-gettemporarygluepartitioncredentials-rest
    port: 8080
    description: REST adapter for Amazon Web Services lakeformation — GetTemporaryGluePartitionCredentials. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/gettemporarygluepartitioncredentials
      name: gettemporarygluepartitioncredentials
      description: REST surface for GetTemporaryGluePartitionCredentials.
      operations:
      - method: POST
        name: amazonwebservicesgettemporarygluepartitioncredentials
        description: Amazon Web Services Gettemporarygluepartitioncredentials
        call: lakeformation-gettemporarygluepartitioncredentials.amazonwebservicesgettemporarygluepartitioncredentials
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: lakeformation-gettemporarygluepartitioncredentials-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services lakeformation — GetTemporaryGluePartitionCredentials. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-gettemporarygluepartitioncredentials
      description: Amazon Web Services Gettemporarygluepartitioncredentials
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: lakeformation-gettemporarygluepartitioncredentials.amazonwebservicesgettemporarygluepartitioncredentials
      outputParameters:
      - type: object
        mapping: $.
