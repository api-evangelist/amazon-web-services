naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services account — enableRegion
  description: 'Amazon Web Services account — enableRegion. 1 operations. Lead operation: Amazon Web Services Enableregion.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - enableRegion
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: account-enableregion
    baseUri: ''
    description: Amazon Web Services account — enableRegion business capability. Self-contained, no shared references.
    resources:
    - name: enableRegion
      path: /enableRegion
      operations:
      - name: amazonwebservicesenableregion
        method: POST
        description: Amazon Web Services Enableregion
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: account-enableregion-rest
    port: 8080
    description: REST adapter for Amazon Web Services account — enableRegion. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/enableregion
      name: enableregion
      description: REST surface for enableRegion.
      operations:
      - method: POST
        name: amazonwebservicesenableregion
        description: Amazon Web Services Enableregion
        call: account-enableregion.amazonwebservicesenableregion
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: account-enableregion-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services account — enableRegion. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: amazon-web-services-enableregion
      description: Amazon Web Services Enableregion
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: account-enableregion.amazonwebservicesenableregion
      outputParameters:
      - type: object
        mapping: $.
