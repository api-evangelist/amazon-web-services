naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services simspaceweaver — Stopclock
  description: 'Amazon Web Services simspaceweaver — Stopclock. 1 operations. Lead operation: Amazon Web Services Stopclock.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Stopclock
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: simspaceweaver-stopclock
    baseUri: ''
    description: Amazon Web Services simspaceweaver — Stopclock business capability. Self-contained, no shared references.
    resources:
    - name: stopclock
      path: /stopclock
      operations:
      - name: amazonwebservicesstopclock
        method: POST
        description: Amazon Web Services Stopclock
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: simspaceweaver-stopclock-rest
    port: 8080
    description: REST adapter for Amazon Web Services simspaceweaver — Stopclock. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/stopclock
      name: stopclock
      description: REST surface for stopclock.
      operations:
      - method: POST
        name: amazonwebservicesstopclock
        description: Amazon Web Services Stopclock
        call: simspaceweaver-stopclock.amazonwebservicesstopclock
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: simspaceweaver-stopclock-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services simspaceweaver — Stopclock. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: amazon-web-services-stopclock
      description: Amazon Web Services Stopclock
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: simspaceweaver-stopclock.amazonwebservicesstopclock
      outputParameters:
      - type: object
        mapping: $.
