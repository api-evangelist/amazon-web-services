naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services codeartifact — Tag
  description: 'Amazon Web Services codeartifact — Tag. 1 operations. Lead operation: Amazon Web Services Tagresource. Self-contained
    Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Tag
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: codeartifact-tag
    baseUri: ''
    description: Amazon Web Services codeartifact — Tag business capability. Self-contained, no shared references.
    resources:
    - name: v1-tag
      path: /v1/tag
      operations:
      - name: amazonwebservicestagresource
        method: POST
        description: Amazon Web Services Tagresource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: codeartifact-tag-rest
    port: 8080
    description: REST adapter for Amazon Web Services codeartifact — Tag. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/v1/tag
      name: v1-tag
      description: REST surface for v1-tag.
      operations:
      - method: POST
        name: amazonwebservicestagresource
        description: Amazon Web Services Tagresource
        call: codeartifact-tag.amazonwebservicestagresource
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: codeartifact-tag-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services codeartifact — Tag. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: amazon-web-services-tagresource
      description: Amazon Web Services Tagresource
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: codeartifact-tag.amazonwebservicestagresource
      outputParameters:
      - type: object
        mapping: $.
