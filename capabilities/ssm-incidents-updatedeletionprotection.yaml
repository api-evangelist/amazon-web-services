naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services ssm-incidents — updateDeletionProtection
  description: 'Amazon Web Services ssm-incidents — updateDeletionProtection. 1 operations. Lead operation: Amazon Web Services
    Updatedeletionprotection. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - updateDeletionProtection
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: ssm-incidents-updatedeletionprotection
    baseUri: ''
    description: Amazon Web Services ssm-incidents — updateDeletionProtection business capability. Self-contained, no shared
      references.
    resources:
    - name: updateDeletionProtection
      path: /updateDeletionProtection
      operations:
      - name: amazonwebservicesupdatedeletionprotection
        method: POST
        description: Amazon Web Services Updatedeletionprotection
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: ssm-incidents-updatedeletionprotection-rest
    port: 8080
    description: REST adapter for Amazon Web Services ssm-incidents — updateDeletionProtection. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/updatedeletionprotection
      name: updatedeletionprotection
      description: REST surface for updateDeletionProtection.
      operations:
      - method: POST
        name: amazonwebservicesupdatedeletionprotection
        description: Amazon Web Services Updatedeletionprotection
        call: ssm-incidents-updatedeletionprotection.amazonwebservicesupdatedeletionprotection
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ssm-incidents-updatedeletionprotection-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services ssm-incidents — updateDeletionProtection. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-updatedeletionprotection
      description: Amazon Web Services Updatedeletionprotection
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ssm-incidents-updatedeletionprotection.amazonwebservicesupdatedeletionprotection
      outputParameters:
      - type: object
        mapping: $.
