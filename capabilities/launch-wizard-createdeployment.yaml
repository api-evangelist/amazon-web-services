naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services launch-wizard — createDeployment
  description: 'Amazon Web Services launch-wizard — createDeployment. 1 operations. Lead operation: Amazon Web Services Createdeployment.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - createDeployment
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: launch-wizard-createdeployment
    baseUri: ''
    description: Amazon Web Services launch-wizard — createDeployment business capability. Self-contained, no shared references.
    resources:
    - name: createDeployment
      path: /createDeployment
      operations:
      - name: amazonwebservicescreatedeployment
        method: POST
        description: Amazon Web Services Createdeployment
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: launch-wizard-createdeployment-rest
    port: 8080
    description: REST adapter for Amazon Web Services launch-wizard — createDeployment. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/createdeployment
      name: createdeployment
      description: REST surface for createDeployment.
      operations:
      - method: POST
        name: amazonwebservicescreatedeployment
        description: Amazon Web Services Createdeployment
        call: launch-wizard-createdeployment.amazonwebservicescreatedeployment
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: launch-wizard-createdeployment-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services launch-wizard — createDeployment. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-createdeployment
      description: Amazon Web Services Createdeployment
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: launch-wizard-createdeployment.amazonwebservicescreatedeployment
      outputParameters:
      - type: object
        mapping: $.
