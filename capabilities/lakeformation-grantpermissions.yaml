naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services lakeformation — GrantPermissions
  description: 'Amazon Web Services lakeformation — GrantPermissions. 1 operations. Lead operation: Amazon Web Services Grantpermissions.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - GrantPermissions
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: lakeformation-grantpermissions
    baseUri: ''
    description: Amazon Web Services lakeformation — GrantPermissions business capability. Self-contained, no shared references.
    resources:
    - name: GrantPermissions
      path: /GrantPermissions
      operations:
      - name: amazonwebservicesgrantpermissions
        method: POST
        description: Amazon Web Services Grantpermissions
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: lakeformation-grantpermissions-rest
    port: 8080
    description: REST adapter for Amazon Web Services lakeformation — GrantPermissions. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/grantpermissions
      name: grantpermissions
      description: REST surface for GrantPermissions.
      operations:
      - method: POST
        name: amazonwebservicesgrantpermissions
        description: Amazon Web Services Grantpermissions
        call: lakeformation-grantpermissions.amazonwebservicesgrantpermissions
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: lakeformation-grantpermissions-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services lakeformation — GrantPermissions. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-grantpermissions
      description: Amazon Web Services Grantpermissions
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: lakeformation-grantpermissions.amazonwebservicesgrantpermissions
      outputParameters:
      - type: object
        mapping: $.
