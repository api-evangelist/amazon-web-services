naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services appflow — List Flows
  description: 'Amazon Web Services appflow — List Flows. 1 operations. Lead operation: Amazon Web Services Listflows. Self-contained
    Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - List Flows
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: appflow-list-flows
    baseUri: ''
    description: Amazon Web Services appflow — List Flows business capability. Self-contained, no shared references.
    resources:
    - name: list-flows
      path: /list-flows
      operations:
      - name: amazonwebserviceslistflows
        method: POST
        description: Amazon Web Services Listflows
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: appflow-list-flows-rest
    port: 8080
    description: REST adapter for Amazon Web Services appflow — List Flows. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/list-flows
      name: list-flows
      description: REST surface for list-flows.
      operations:
      - method: POST
        name: amazonwebserviceslistflows
        description: Amazon Web Services Listflows
        call: appflow-list-flows.amazonwebserviceslistflows
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: appflow-list-flows-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services appflow — List Flows. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: amazon-web-services-listflows
      description: Amazon Web Services Listflows
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: appflow-list-flows.amazonwebserviceslistflows
      outputParameters:
      - type: object
        mapping: $.
