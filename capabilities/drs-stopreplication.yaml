naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services drs — StopReplication
  description: 'Amazon Web Services drs — StopReplication. 1 operations. Lead operation: Amazon Web Services Stopreplication.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - StopReplication
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: drs-stopreplication
    baseUri: ''
    description: Amazon Web Services drs — StopReplication business capability. Self-contained, no shared references.
    resources:
    - name: StopReplication
      path: /StopReplication
      operations:
      - name: amazonwebservicesstopreplication
        method: POST
        description: Amazon Web Services Stopreplication
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: drs-stopreplication-rest
    port: 8080
    description: REST adapter for Amazon Web Services drs — StopReplication. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/stopreplication
      name: stopreplication
      description: REST surface for StopReplication.
      operations:
      - method: POST
        name: amazonwebservicesstopreplication
        description: Amazon Web Services Stopreplication
        call: drs-stopreplication.amazonwebservicesstopreplication
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: drs-stopreplication-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services drs — StopReplication. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: amazon-web-services-stopreplication
      description: Amazon Web Services Stopreplication
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: drs-stopreplication.amazonwebservicesstopreplication
      outputParameters:
      - type: object
        mapping: $.
