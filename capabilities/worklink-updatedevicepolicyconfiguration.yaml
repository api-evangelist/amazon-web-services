naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services worklink — updateDevicePolicyConfiguration
  description: 'Amazon Web Services worklink — updateDevicePolicyConfiguration. 1 operations. Lead operation: Amazon Web Services
    Updatedevicepolicyconfiguration. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - updateDevicePolicyConfiguration
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: worklink-updatedevicepolicyconfiguration
    baseUri: ''
    description: Amazon Web Services worklink — updateDevicePolicyConfiguration business capability. Self-contained, no shared
      references.
    resources:
    - name: updateDevicePolicyConfiguration
      path: /updateDevicePolicyConfiguration
      operations:
      - name: amazonwebservicesupdatedevicepolicyconfiguration
        method: POST
        description: Amazon Web Services Updatedevicepolicyconfiguration
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: worklink-updatedevicepolicyconfiguration-rest
    port: 8080
    description: REST adapter for Amazon Web Services worklink — updateDevicePolicyConfiguration. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/updatedevicepolicyconfiguration
      name: updatedevicepolicyconfiguration
      description: REST surface for updateDevicePolicyConfiguration.
      operations:
      - method: POST
        name: amazonwebservicesupdatedevicepolicyconfiguration
        description: Amazon Web Services Updatedevicepolicyconfiguration
        call: worklink-updatedevicepolicyconfiguration.amazonwebservicesupdatedevicepolicyconfiguration
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: worklink-updatedevicepolicyconfiguration-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services worklink — updateDevicePolicyConfiguration. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-updatedevicepolicyconfiguration
      description: Amazon Web Services Updatedevicepolicyconfiguration
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: worklink-updatedevicepolicyconfiguration.amazonwebservicesupdatedevicepolicyconfiguration
      outputParameters:
      - type: object
        mapping: $.
