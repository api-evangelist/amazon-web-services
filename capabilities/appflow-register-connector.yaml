naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services appflow — Register Connector
  description: 'Amazon Web Services appflow — Register Connector. 1 operations. Lead operation: Amazon Web Services Registerconnector.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Register Connector
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: appflow-register-connector
    baseUri: ''
    description: Amazon Web Services appflow — Register Connector business capability. Self-contained, no shared references.
    resources:
    - name: register-connector
      path: /register-connector
      operations:
      - name: amazonwebservicesregisterconnector
        method: POST
        description: Amazon Web Services Registerconnector
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: appflow-register-connector-rest
    port: 8080
    description: REST adapter for Amazon Web Services appflow — Register Connector. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/register-connector
      name: register-connector
      description: REST surface for register-connector.
      operations:
      - method: POST
        name: amazonwebservicesregisterconnector
        description: Amazon Web Services Registerconnector
        call: appflow-register-connector.amazonwebservicesregisterconnector
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: appflow-register-connector-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services appflow — Register Connector. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-registerconnector
      description: Amazon Web Services Registerconnector
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: appflow-register-connector.amazonwebservicesregisterconnector
      outputParameters:
      - type: object
        mapping: $.
