naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services drs — StartReplication
  description: 'Amazon Web Services drs — StartReplication. 1 operations. Lead operation: Amazon Web Services Startreplication.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - StartReplication
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: drs-startreplication
    baseUri: ''
    description: Amazon Web Services drs — StartReplication business capability. Self-contained, no shared references.
    resources:
    - name: StartReplication
      path: /StartReplication
      operations:
      - name: amazonwebservicesstartreplication
        method: POST
        description: Amazon Web Services Startreplication
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: drs-startreplication-rest
    port: 8080
    description: REST adapter for Amazon Web Services drs — StartReplication. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/startreplication
      name: startreplication
      description: REST surface for StartReplication.
      operations:
      - method: POST
        name: amazonwebservicesstartreplication
        description: Amazon Web Services Startreplication
        call: drs-startreplication.amazonwebservicesstartreplication
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: drs-startreplication-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services drs — StartReplication. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: amazon-web-services-startreplication
      description: Amazon Web Services Startreplication
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: drs-startreplication.amazonwebservicesstartreplication
      outputParameters:
      - type: object
        mapping: $.
