naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services drs — InitializeService
  description: 'Amazon Web Services drs — InitializeService. 1 operations. Lead operation: Amazon Web Services Initializeservice.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - InitializeService
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: drs-initializeservice
    baseUri: ''
    description: Amazon Web Services drs — InitializeService business capability. Self-contained, no shared references.
    resources:
    - name: InitializeService
      path: /InitializeService
      operations:
      - name: amazonwebservicesinitializeservice
        method: POST
        description: Amazon Web Services Initializeservice
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: drs-initializeservice-rest
    port: 8080
    description: REST adapter for Amazon Web Services drs — InitializeService. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/initializeservice
      name: initializeservice
      description: REST surface for InitializeService.
      operations:
      - method: POST
        name: amazonwebservicesinitializeservice
        description: Amazon Web Services Initializeservice
        call: drs-initializeservice.amazonwebservicesinitializeservice
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: drs-initializeservice-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services drs — InitializeService. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: amazon-web-services-initializeservice
      description: Amazon Web Services Initializeservice
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: drs-initializeservice.amazonwebservicesinitializeservice
      outputParameters:
      - type: object
        mapping: $.
