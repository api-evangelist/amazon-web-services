naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services migrationhuborchestrator — Templates
  description: 'Amazon Web Services migrationhuborchestrator — Templates. 1 operations. Lead operation: Amazon Web Services
    Gettemplatestepgroup. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Templates
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: migrationhuborchestrator-templates
    baseUri: ''
    description: Amazon Web Services migrationhuborchestrator — Templates business capability. Self-contained, no shared references.
    resources:
    - name: templates-templateId-stepgroups-id
      path: /templates/{templateId}/stepgroups/{id}
      operations:
      - name: amazonwebservicesgettemplatestepgroup
        method: GET
        description: Amazon Web Services Gettemplatestepgroup
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: migrationhuborchestrator-templates-rest
    port: 8080
    description: REST adapter for Amazon Web Services migrationhuborchestrator — Templates. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/templates/{templateid}/stepgroups/{id}
      name: templates-templateid-stepgroups-id
      description: REST surface for templates-templateId-stepgroups-id.
      operations:
      - method: GET
        name: amazonwebservicesgettemplatestepgroup
        description: Amazon Web Services Gettemplatestepgroup
        call: migrationhuborchestrator-templates.amazonwebservicesgettemplatestepgroup
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: migrationhuborchestrator-templates-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services migrationhuborchestrator — Templates. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-gettemplatestepgroup
      description: Amazon Web Services Gettemplatestepgroup
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: migrationhuborchestrator-templates.amazonwebservicesgettemplatestepgroup
      outputParameters:
      - type: object
        mapping: $.
