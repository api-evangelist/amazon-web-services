naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services batch — Listjobs
  description: 'Amazon Web Services batch — Listjobs. 1 operations. Lead operation: Amazon Web Services Listjobs. Self-contained
    Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Listjobs
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: batch-listjobs
    baseUri: ''
    description: Amazon Web Services batch — Listjobs business capability. Self-contained, no shared references.
    resources:
    - name: v1-listjobs
      path: /v1/listjobs
      operations:
      - name: amazonwebserviceslistjobs
        method: POST
        description: Amazon Web Services Listjobs
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: batch-listjobs-rest
    port: 8080
    description: REST adapter for Amazon Web Services batch — Listjobs. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/v1/listjobs
      name: v1-listjobs
      description: REST surface for v1-listjobs.
      operations:
      - method: POST
        name: amazonwebserviceslistjobs
        description: Amazon Web Services Listjobs
        call: batch-listjobs.amazonwebserviceslistjobs
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: batch-listjobs-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services batch — Listjobs. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: amazon-web-services-listjobs
      description: Amazon Web Services Listjobs
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: batch-listjobs.amazonwebserviceslistjobs
      outputParameters:
      - type: object
        mapping: $.
