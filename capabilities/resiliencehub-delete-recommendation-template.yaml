naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services resiliencehub — Delete Recommendation Template
  description: 'Amazon Web Services resiliencehub — Delete Recommendation Template. 1 operations. Lead operation: Amazon Web
    Services Deleterecommendationtemplate. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Delete Recommendation Template
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: resiliencehub-delete-recommendation-template
    baseUri: ''
    description: Amazon Web Services resiliencehub — Delete Recommendation Template business capability. Self-contained, no
      shared references.
    resources:
    - name: delete-recommendation-template
      path: /delete-recommendation-template
      operations:
      - name: amazonwebservicesdeleterecommendationtemplate
        method: POST
        description: Amazon Web Services Deleterecommendationtemplate
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: resiliencehub-delete-recommendation-template-rest
    port: 8080
    description: REST adapter for Amazon Web Services resiliencehub — Delete Recommendation Template. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/delete-recommendation-template
      name: delete-recommendation-template
      description: REST surface for delete-recommendation-template.
      operations:
      - method: POST
        name: amazonwebservicesdeleterecommendationtemplate
        description: Amazon Web Services Deleterecommendationtemplate
        call: resiliencehub-delete-recommendation-template.amazonwebservicesdeleterecommendationtemplate
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: resiliencehub-delete-recommendation-template-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services resiliencehub — Delete Recommendation Template. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-deleterecommendationtemplate
      description: Amazon Web Services Deleterecommendationtemplate
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: resiliencehub-delete-recommendation-template.amazonwebservicesdeleterecommendationtemplate
      outputParameters:
      - type: object
        mapping: $.
