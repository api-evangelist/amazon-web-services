naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services transcribe-streaming — Stream Transcription
  description: 'Amazon Web Services transcribe-streaming — Stream Transcription. 1 operations. Lead operation: Amazon Web
    Services Startstreamtranscription. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Stream Transcription
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: transcribe-streaming-stream-transcription
    baseUri: ''
    description: Amazon Web Services transcribe-streaming — Stream Transcription business capability. Self-contained, no shared
      references.
    resources:
    - name: stream-transcription
      path: /stream-transcription
      operations:
      - name: amazonwebservicesstartstreamtranscription
        method: POST
        description: Amazon Web Services Startstreamtranscription
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: transcribe-streaming-stream-transcription-rest
    port: 8080
    description: REST adapter for Amazon Web Services transcribe-streaming — Stream Transcription. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/stream-transcription
      name: stream-transcription
      description: REST surface for stream-transcription.
      operations:
      - method: POST
        name: amazonwebservicesstartstreamtranscription
        description: Amazon Web Services Startstreamtranscription
        call: transcribe-streaming-stream-transcription.amazonwebservicesstartstreamtranscription
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: transcribe-streaming-stream-transcription-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services transcribe-streaming — Stream Transcription. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-startstreamtranscription
      description: Amazon Web Services Startstreamtranscription
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: transcribe-streaming-stream-transcription.amazonwebservicesstartstreamtranscription
      outputParameters:
      - type: object
        mapping: $.
