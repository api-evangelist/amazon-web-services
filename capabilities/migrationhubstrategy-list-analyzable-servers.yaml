naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services migrationhubstrategy — List Analyzable Servers
  description: 'Amazon Web Services migrationhubstrategy — List Analyzable Servers. 1 operations. Lead operation: Amazon Web
    Services Listanalyzableservers. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - List Analyzable Servers
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: migrationhubstrategy-list-analyzable-servers
    baseUri: ''
    description: Amazon Web Services migrationhubstrategy — List Analyzable Servers business capability. Self-contained, no
      shared references.
    resources:
    - name: list-analyzable-servers
      path: /list-analyzable-servers
      operations:
      - name: amazonwebserviceslistanalyzableservers
        method: POST
        description: Amazon Web Services Listanalyzableservers
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: migrationhubstrategy-list-analyzable-servers-rest
    port: 8080
    description: REST adapter for Amazon Web Services migrationhubstrategy — List Analyzable Servers. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/list-analyzable-servers
      name: list-analyzable-servers
      description: REST surface for list-analyzable-servers.
      operations:
      - method: POST
        name: amazonwebserviceslistanalyzableservers
        description: Amazon Web Services Listanalyzableservers
        call: migrationhubstrategy-list-analyzable-servers.amazonwebserviceslistanalyzableservers
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: migrationhubstrategy-list-analyzable-servers-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services migrationhubstrategy — List Analyzable Servers. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-listanalyzableservers
      description: Amazon Web Services Listanalyzableservers
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: migrationhubstrategy-list-analyzable-servers.amazonwebserviceslistanalyzableservers
      outputParameters:
      - type: object
        mapping: $.
