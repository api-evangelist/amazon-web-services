naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services lakeformation — ExtendTransaction
  description: 'Amazon Web Services lakeformation — ExtendTransaction. 1 operations. Lead operation: Amazon Web Services Extendtransaction.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - ExtendTransaction
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: lakeformation-extendtransaction
    baseUri: ''
    description: Amazon Web Services lakeformation — ExtendTransaction business capability. Self-contained, no shared references.
    resources:
    - name: ExtendTransaction
      path: /ExtendTransaction
      operations:
      - name: amazonwebservicesextendtransaction
        method: POST
        description: Amazon Web Services Extendtransaction
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: lakeformation-extendtransaction-rest
    port: 8080
    description: REST adapter for Amazon Web Services lakeformation — ExtendTransaction. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/extendtransaction
      name: extendtransaction
      description: REST surface for ExtendTransaction.
      operations:
      - method: POST
        name: amazonwebservicesextendtransaction
        description: Amazon Web Services Extendtransaction
        call: lakeformation-extendtransaction.amazonwebservicesextendtransaction
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: lakeformation-extendtransaction-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services lakeformation — ExtendTransaction. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-extendtransaction
      description: Amazon Web Services Extendtransaction
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: lakeformation-extendtransaction.amazonwebservicesextendtransaction
      outputParameters:
      - type: object
        mapping: $.
