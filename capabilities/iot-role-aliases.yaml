naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services iot — Role Aliases
  description: 'Amazon Web Services iot — Role Aliases. 2 operations. Lead operation: Amazon Web Services Listrolealiases.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Role Aliases
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: iot-role-aliases
    baseUri: ''
    description: Amazon Web Services iot — Role Aliases business capability. Self-contained, no shared references.
    resources:
    - name: role-aliases
      path: /role-aliases
      operations:
      - name: amazonwebserviceslistrolealiases
        method: GET
        description: Amazon Web Services Listrolealiases
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: role-aliases-roleAlias
      path: /role-aliases/{roleAlias}
      operations:
      - name: amazonwebservicesupdaterolealias
        method: PUT
        description: Amazon Web Services Updaterolealias
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: iot-role-aliases-rest
    port: 8080
    description: REST adapter for Amazon Web Services iot — Role Aliases. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/role-aliases
      name: role-aliases
      description: REST surface for role-aliases.
      operations:
      - method: GET
        name: amazonwebserviceslistrolealiases
        description: Amazon Web Services Listrolealiases
        call: iot-role-aliases.amazonwebserviceslistrolealiases
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/role-aliases/{rolealias}
      name: role-aliases-rolealias
      description: REST surface for role-aliases-roleAlias.
      operations:
      - method: PUT
        name: amazonwebservicesupdaterolealias
        description: Amazon Web Services Updaterolealias
        call: iot-role-aliases.amazonwebservicesupdaterolealias
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: iot-role-aliases-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services iot — Role Aliases. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: amazon-web-services-listrolealiases
      description: Amazon Web Services Listrolealiases
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: iot-role-aliases.amazonwebserviceslistrolealiases
      outputParameters:
      - type: object
        mapping: $.
    - name: amazon-web-services-updaterolealias
      description: Amazon Web Services Updaterolealias
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: iot-role-aliases.amazonwebservicesupdaterolealias
      outputParameters:
      - type: object
        mapping: $.
