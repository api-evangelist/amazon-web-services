naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services ssm-sap — Register Application
  description: 'Amazon Web Services ssm-sap — Register Application. 1 operations. Lead operation: Amazon Web Services Registerapplication.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Register Application
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: ssm-sap-register-application
    baseUri: ''
    description: Amazon Web Services ssm-sap — Register Application business capability. Self-contained, no shared references.
    resources:
    - name: register-application
      path: /register-application
      operations:
      - name: amazonwebservicesregisterapplication
        method: POST
        description: Amazon Web Services Registerapplication
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: ssm-sap-register-application-rest
    port: 8080
    description: REST adapter for Amazon Web Services ssm-sap — Register Application. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/register-application
      name: register-application
      description: REST surface for register-application.
      operations:
      - method: POST
        name: amazonwebservicesregisterapplication
        description: Amazon Web Services Registerapplication
        call: ssm-sap-register-application.amazonwebservicesregisterapplication
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ssm-sap-register-application-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services ssm-sap — Register Application. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-registerapplication
      description: Amazon Web Services Registerapplication
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ssm-sap-register-application.amazonwebservicesregisterapplication
      outputParameters:
      - type: object
        mapping: $.
