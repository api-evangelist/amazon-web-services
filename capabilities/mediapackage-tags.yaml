naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services mediapackage — Tags
  description: 'Amazon Web Services mediapackage — Tags. 1 operations. Lead operation: Amazon Web Services Untagresource.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Tags
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: mediapackage-tags
    baseUri: ''
    description: Amazon Web Services mediapackage — Tags business capability. Self-contained, no shared references.
    resources:
    - name: tags-resource-arn
      path: /tags/{resource-arn}
      operations:
      - name: amazonwebservicesuntagresource
        method: DELETE
        description: Amazon Web Services Untagresource
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: mediapackage-tags-rest
    port: 8080
    description: REST adapter for Amazon Web Services mediapackage — Tags. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/tags/{resource-arn}
      name: tags-resource-arn
      description: REST surface for tags-resource-arn.
      operations:
      - method: DELETE
        name: amazonwebservicesuntagresource
        description: Amazon Web Services Untagresource
        call: mediapackage-tags.amazonwebservicesuntagresource
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: mediapackage-tags-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services mediapackage — Tags. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: amazon-web-services-untagresource
      description: Amazon Web Services Untagresource
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: mediapackage-tags.amazonwebservicesuntagresource
      outputParameters:
      - type: object
        mapping: $.
