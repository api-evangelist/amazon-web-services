naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services launch-wizard — getDeployment
  description: 'Amazon Web Services launch-wizard — getDeployment. 1 operations. Lead operation: Amazon Web Services Getdeployment.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - getDeployment
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: launch-wizard-getdeployment
    baseUri: ''
    description: Amazon Web Services launch-wizard — getDeployment business capability. Self-contained, no shared references.
    resources:
    - name: getDeployment
      path: /getDeployment
      operations:
      - name: amazonwebservicesgetdeployment
        method: POST
        description: Amazon Web Services Getdeployment
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: launch-wizard-getdeployment-rest
    port: 8080
    description: REST adapter for Amazon Web Services launch-wizard — getDeployment. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/getdeployment
      name: getdeployment
      description: REST surface for getDeployment.
      operations:
      - method: POST
        name: amazonwebservicesgetdeployment
        description: Amazon Web Services Getdeployment
        call: launch-wizard-getdeployment.amazonwebservicesgetdeployment
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: launch-wizard-getdeployment-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services launch-wizard — getDeployment. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-getdeployment
      description: Amazon Web Services Getdeployment
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: launch-wizard-getdeployment.amazonwebservicesgetdeployment
      outputParameters:
      - type: object
        mapping: $.
