naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services rds-data — ExecuteSql
  description: 'Amazon Web Services rds-data — ExecuteSql. 1 operations. Lead operation: Amazon Web Services Executesql. Self-contained
    Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - ExecuteSql
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: rds-data-executesql
    baseUri: ''
    description: Amazon Web Services rds-data — ExecuteSql business capability. Self-contained, no shared references.
    resources:
    - name: ExecuteSql
      path: /ExecuteSql
      operations:
      - name: amazonwebservicesexecutesql
        method: POST
        description: Amazon Web Services Executesql
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: rds-data-executesql-rest
    port: 8080
    description: REST adapter for Amazon Web Services rds-data — ExecuteSql. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/executesql
      name: executesql
      description: REST surface for ExecuteSql.
      operations:
      - method: POST
        name: amazonwebservicesexecutesql
        description: Amazon Web Services Executesql
        call: rds-data-executesql.amazonwebservicesexecutesql
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: rds-data-executesql-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services rds-data — ExecuteSql. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: amazon-web-services-executesql
      description: Amazon Web Services Executesql
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: rds-data-executesql.amazonwebservicesexecutesql
      outputParameters:
      - type: object
        mapping: $.
