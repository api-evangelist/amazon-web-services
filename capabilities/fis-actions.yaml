naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services fis — Actions
  description: 'Amazon Web Services fis — Actions. 2 operations. Lead operation: Amazon Web Services Listactions. Self-contained
    Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Actions
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: fis-actions
    baseUri: ''
    description: Amazon Web Services fis — Actions business capability. Self-contained, no shared references.
    resources:
    - name: actions
      path: /actions
      operations:
      - name: amazonwebserviceslistactions
        method: GET
        description: Amazon Web Services Listactions
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: actions-id
      path: /actions/{id}
      operations:
      - name: amazonwebservicesgetaction
        method: GET
        description: Amazon Web Services Getaction
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: fis-actions-rest
    port: 8080
    description: REST adapter for Amazon Web Services fis — Actions. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/actions
      name: actions
      description: REST surface for actions.
      operations:
      - method: GET
        name: amazonwebserviceslistactions
        description: Amazon Web Services Listactions
        call: fis-actions.amazonwebserviceslistactions
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/actions/{id}
      name: actions-id
      description: REST surface for actions-id.
      operations:
      - method: GET
        name: amazonwebservicesgetaction
        description: Amazon Web Services Getaction
        call: fis-actions.amazonwebservicesgetaction
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: fis-actions-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services fis — Actions. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: amazon-web-services-listactions
      description: Amazon Web Services Listactions
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fis-actions.amazonwebserviceslistactions
      outputParameters:
      - type: object
        mapping: $.
    - name: amazon-web-services-getaction
      description: Amazon Web Services Getaction
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fis-actions.amazonwebservicesgetaction
      outputParameters:
      - type: object
        mapping: $.
