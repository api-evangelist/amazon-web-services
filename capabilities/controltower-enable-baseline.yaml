naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services controltower — Enable Baseline
  description: 'Amazon Web Services controltower — Enable Baseline. 1 operations. Lead operation: Amazon Web Services Enablebaseline.
    Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Enable Baseline
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: controltower-enable-baseline
    baseUri: ''
    description: Amazon Web Services controltower — Enable Baseline business capability. Self-contained, no shared references.
    resources:
    - name: enable-baseline
      path: /enable-baseline
      operations:
      - name: amazonwebservicesenablebaseline
        method: POST
        description: Amazon Web Services Enablebaseline
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: controltower-enable-baseline-rest
    port: 8080
    description: REST adapter for Amazon Web Services controltower — Enable Baseline. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/enable-baseline
      name: enable-baseline
      description: REST surface for enable-baseline.
      operations:
      - method: POST
        name: amazonwebservicesenablebaseline
        description: Amazon Web Services Enablebaseline
        call: controltower-enable-baseline.amazonwebservicesenablebaseline
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: controltower-enable-baseline-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services controltower — Enable Baseline. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-enablebaseline
      description: Amazon Web Services Enablebaseline
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: controltower-enable-baseline.amazonwebservicesenablebaseline
      outputParameters:
      - type: object
        mapping: $.
