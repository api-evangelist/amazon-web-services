naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services AWS S3 — Bucket Lifecycle Configuration
  description: 'Amazon Web Services AWS S3 — Bucket Lifecycle Configuration. 1 operations. Lead operation: Amazon Web Services
    Put Bucket Lifecycle Configuration. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - Bucket Lifecycle Configuration
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: s3-bucket-lifecycle-configuration
    baseUri: ''
    description: Amazon Web Services AWS S3 — Bucket Lifecycle Configuration business capability. Self-contained, no shared
      references.
    resources:
    - name: Bucket}?lifecycle
      path: /{Bucket}?lifecycle
      operations:
      - name: putbucketlifecycleconfiguration
        method: PUT
        description: Amazon Web Services Put Bucket Lifecycle Configuration
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: s3-bucket-lifecycle-configuration-rest
    port: 8080
    description: REST adapter for Amazon Web Services AWS S3 — Bucket Lifecycle Configuration. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/bucket-lifecycle
      name: bucket-lifecycle
      description: REST surface for Bucket}?lifecycle.
      operations:
      - method: PUT
        name: putbucketlifecycleconfiguration
        description: Amazon Web Services Put Bucket Lifecycle Configuration
        call: s3-bucket-lifecycle-configuration.putbucketlifecycleconfiguration
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: s3-bucket-lifecycle-configuration-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services AWS S3 — Bucket Lifecycle Configuration. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-put-bucket
      description: Amazon Web Services Put Bucket Lifecycle Configuration
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: s3-bucket-lifecycle-configuration.putbucketlifecycleconfiguration
      outputParameters:
      - type: object
        mapping: $.
