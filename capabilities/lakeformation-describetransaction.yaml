naftiko: 1.0.0-alpha2
info:
  label: Amazon Web Services lakeformation — DescribeTransaction
  description: 'Amazon Web Services lakeformation — DescribeTransaction. 1 operations. Lead operation: Amazon Web Services
    Describetransaction. Self-contained Naftiko capability covering one Amazon Web Services business surface.'
  tags:
  - Amazon Web Services
  - DescribeTransaction
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AMAZON_WEB_SERVICES_API_KEY: AMAZON_WEB_SERVICES_API_KEY
capability:
  consumes:
  - type: http
    namespace: lakeformation-describetransaction
    baseUri: ''
    description: Amazon Web Services lakeformation — DescribeTransaction business capability. Self-contained, no shared references.
    resources:
    - name: DescribeTransaction
      path: /DescribeTransaction
      operations:
      - name: amazonwebservicesdescribetransaction
        method: POST
        description: Amazon Web Services Describetransaction
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: lakeformation-describetransaction-rest
    port: 8080
    description: REST adapter for Amazon Web Services lakeformation — DescribeTransaction. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/describetransaction
      name: describetransaction
      description: REST surface for DescribeTransaction.
      operations:
      - method: POST
        name: amazonwebservicesdescribetransaction
        description: Amazon Web Services Describetransaction
        call: lakeformation-describetransaction.amazonwebservicesdescribetransaction
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: lakeformation-describetransaction-mcp
    port: 9090
    transport: http
    description: MCP adapter for Amazon Web Services lakeformation — DescribeTransaction. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: amazon-web-services-describetransaction
      description: Amazon Web Services Describetransaction
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: lakeformation-describetransaction.amazonwebservicesdescribetransaction
      outputParameters:
      - type: object
        mapping: $.
