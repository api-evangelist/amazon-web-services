openapi: 3.2.0
info:
  title: Amazon Web Services drs Stop Source Network Replication API
  description: AWS Elastic Disaster Recovery Service.
tags:
- name: StopSourceNetworkReplication
paths:
  /StopSourceNetworkReplication:
    POST:
      summary: Amazon Web Services Stopsourcenetworkreplication
      description: Stops replication for a Source Network. This action would make the Source Network unprotected.
      operationId: amazonWebServicesStopSourceNetworkReplication
      tags:
      - StopSourceNetworkReplication
