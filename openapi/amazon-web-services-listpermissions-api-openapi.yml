openapi: 3.2.0
info:
  title: Amazon Web Services List Permissions API
  version: '1.0'
  description: 'Operations tagged ListPermissions across 2 of this provider''s published API definitions: lakeformation-openapi-original.yml, ram-openapi-original.yml. Each path carries the servers of the definition it was published in.'
tags:
- name: ListPermissions
paths:
  /ListPermissions:
    POST:
      summary: Amazon Web Services Listpermissions
      description: Returns a list of the principal permissions on the resource, filtered by the permissions of the caller. For example, if you are granted an ALTER permission, you are able to see only the principal permissions for ALTER. This operation returns only those permissions that have been explicitly granted. For information about permissions, see Security and Access Control to Metadata and Data.
      operationId: amazonWebServicesListPermissions
      tags:
      - ListPermissions
  /listpermissions:
    POST:
      summary: Amazon Web Services Listpermissions
      description: Retrieves a list of available RAM permissions that you can use for the supported resource types.
      operationId: amazonWebServicesListPermissions
      tags:
      - ListPermissions
x-refined-from:
- lakeformation-openapi-original.yml
- ram-openapi-original.yml
