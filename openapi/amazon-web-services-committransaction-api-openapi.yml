openapi: 3.2.0
info:
  title: Amazon Web Services Commit Transaction API
  version: '1.0'
  description: 'Operations tagged CommitTransaction across 2 of this provider''s published API definitions: lakeformation-openapi-original.yml, rds-data-openapi-original.yml. Each path carries the servers of the definition it was published in.'
tags:
- name: CommitTransaction
paths:
  /CommitTransaction:
    POST:
      summary: Amazon Web Services Committransaction
      description: Attempts to commit the specified transaction. Returns an exception if the transaction was previously aborted. This API action is idempotent if called multiple times for the same transaction.
      operationId: amazonWebServicesCommitTransaction
      tags:
      - CommitTransaction
x-refined-from:
- lakeformation-openapi-original.yml
- rds-data-openapi-original.yml
