openapi: 3.2.0
info:
  title: Amazon Web Services mediatailor Source Locations API
  description: Use the AWS Elemental MediaTailor SDKs and CLI to configure scalable ad insertion and linear channels.
tags:
- name: Source Locations
paths:
  /sourceLocations:
    GET:
      summary: Amazon Web Services Listsourcelocations
      description: Lists the source locations for a channel. A source location defines the host server URL, and contains a list of sources.
      operationId: amazonWebServicesListSourceLocations
      tags:
      - Source Locations
