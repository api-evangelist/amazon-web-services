openapi: 3.2.0
info:
  title: Amazon Web Services Start Replication API
  version: '1.0'
  description: 'Operations tagged StartReplication across 2 of this provider''s published API definitions: drs-openapi-original.yml, mgn-openapi-original.yml. Each path carries the servers of the definition it was published in.'
tags:
- name: StartReplication
paths:
  /StartReplication:
    POST:
      summary: Amazon Web Services Startreplication
      description: Starts replication for a stopped Source Server. This action would make the Source Server protected again and restart billing for it.
      operationId: amazonWebServicesStartReplication
      tags:
      - StartReplication
x-refined-from:
- drs-openapi-original.yml
- mgn-openapi-original.yml
