openapi: 3.2.0
info:
  title: Amazon Web Services appsync Schema API
  description: AppSync provides API actions for creating and interacting with data sources using GraphQL from your application.
tags:
- name: Schema
paths:
  /v1/apis/{apiId}/schema:
    GET:
      summary: Amazon Web Services Getintrospectionschema
      description: Retrieves the introspection schema for a GraphQL API.
      operationId: amazonWebServicesGetIntrospectionSchema
      tags:
      - Schema
