openapi: 3.2.0
info:
  title: Amazon Web Services Stop Replication API
  version: '1.0'
  description: 'Operations tagged StopReplication across 2 of this provider''s published API definitions: drs-openapi-original.yml, mgn-openapi-original.yml. Each path carries the servers of the definition it was published in.'
tags:
- name: StopReplication
paths:
  /StopReplication:
    POST:
      summary: Amazon Web Services Stopreplication
      description: Stops replication for a Source Server. This action would make the Source Server unprotected, delete its existing snapshots and stop billing for it.
      operationId: amazonWebServicesStopReplication
      tags:
      - StopReplication
x-refined-from:
- drs-openapi-original.yml
- mgn-openapi-original.yml
